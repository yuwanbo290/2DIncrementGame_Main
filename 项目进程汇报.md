# 项目进程汇报

> 本文件只保留当前状态与近期架构决策；完整修改历史使用 Git 查询，避免在两处重复维护。

## 当前状态（2026-08-28）

- 引擎：Godot 4.7，GDScript，入口为 `Scenes/GameManager.tscn`。
- 数据：`data/*.xlsx` 五段式源表，经 `addons/table_exporter` 导出到 `Resources/Tables/*.tres`。
- UI：全屏界面继承 `UIBase`；`_ready()` 绑定信号，`refresh()` 刷新动态数据；导航与缓存由 `UIManager` 管理。
- 按钮：统一使用 Godot `Button` 和 `Resources/button_theme.tres`，动态按钮通过 `UIBase._create_text_button()` 创建。
- 存档：`user://save.json`，3 个槽位；槽位界面复用 `Scenes/ui/save_slot.tscn`。
- 战斗：`Area2D` 负责子弹/敌人碰撞，`BaseConfig.duplicate(true)` 提供每局独立配置，提示与伤害数字使用 Tween。
- 波次：普通敌人按 `generateProbability.weight` 抽取；每波使用唯一 `waveBoss` 行，达到 `CreateCost` 后生成 Boss；**击败 Boss 后弹出「进入下一波 / 继续当前」双按钮并暂停时间**：点「进入下一波」清除场上残留敌人后推进（最后一波为「完成讨伐」结算），点「继续当前」留在本波刷怪直至体力耗尽。
- 刷怪：场上全灭会立即补一批，`max_enemies` 限制同时存在数量；敌人在屏幕内随机游走并于边界反弹。
- 属性：`PlayerStatsService` 统一武器界面和战斗暂停菜单的玩家属性；暴击率溢出按 1:1.5 转为暴击伤害。
- 交互：商店、武器与局外养成列表复用 `DragScroll`；Buff 卡片显示「上一级 → 下一级」配置文本。
- 波次事件：进入下一波时按 `waveEvents.weight` 随机触发（雷暴/火山/瘟疫/老虎机），开场动画后按 `Resources/Events/wave_event_<id>.tres` 配置生效（限时）；数值改 `.tres` 即可配平。
- 敌人数据：运行时字段统一使用 `enemy` 语义（含 `enemyName`），题材描述文本仍可保留“哥布林”。

## 当前开发规则

- 开发前阅读 `AI开发规范.md` 和 `接口文档.md`；本文件只用于快速了解当前状态。
- 表结构由用户决定；修改数据必须改 xlsx 源表并重新导表，不直接编辑生成的 tres。
- 局内 Buff、临时战斗配置不写存档；局外金币、技能、武器和统计必须落盘。
- 不添加未被当前需求使用的兼容层、预留分支或抽象。
- 接口或架构发生变化时同步更新 `接口文档.md`；Git 提交信息记录修改原因。

## 查询历史

```bash
git log --oneline --decorate
git show <commit>
```
