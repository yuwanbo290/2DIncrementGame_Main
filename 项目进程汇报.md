# 项目进程汇报

> 用途：记录本项目每次修改，供后续会话快速了解历史、避免重复犯错。
> 约定：每次修改前先读本文件；修改后追加「修改历史」并同步更新相关规则。

---

## 一、关键规则（务必遵守）

- **修改前必须先读本文件 + 接口文档.md**，修改后追加「修改历史」并同步更新接口文档（用户明确要求）
- **不添加与需求无关的冗余文件**（用户明确强调）
- 表结构由用户设定；**只有拿到对应模块的表结构后才开发该模块**
- 基础设置存 `Resources/Config/base_config.tres`（Godot 资源，非 JSON）
- 存档用 `user://save.json` + `FileAccess`
- 武器系统完全重写，旧文件删除；枪械走表；攻击方式 = 按住左键持续射击
- UI 框架复用 `UIBase.gd` / `UIManager.gd`（不改生命周期）
- **TextureButton 子节点必须 `mouse_filter = 2`**（否则拦截点击）
- **UI 导航必须通过 UIManager**（`open_ui`/`close_ui`），战斗场景除外
- **Godot 4.x**：`SceneTree` 无 `has_node()`，用 `get_tree().root.has_node()`
- **Variant 返回值必须显式类型声明**（`Dictionary.get()`、`JSON.parse_string()` 等），不能用 `:=`

## 二、导表工具（addons/table_exporter）

- 输入 `data/*.csv` → 输出 `Resources/Tables/<表名>.tres`
- 入口：编辑器工具栏「导表」按钮 / `项目 > 工具 > 导表：CSV → .tres`
- **CSV 格式**：第1行字段名 / 第2行类型 / 第3行中文备注 / 第4行默认值（后两行导表时跳过，必须保留）/ 第5行起数据
- **可重复 Id**：表行用 `Array[Dictionary]` 保存；查询用 `TableDB.get_all/get_first/rows_of`
- 类型转换：int（空→0）、float（空→0.0）、bool、string（空→""）

## 三、数据表结构（用户提供 4 张）

| 表 | 字段（类型） |
|---|---|
| Ducks | duckID:int · duckName:string · healthNum:float · coin:float · moveSpeed:float · texture:string · spriteFrames:string |
| Skill | Id:int · skillName:string · desc:string · previouId:int · maxLevel:int |
| generateProbability | waveNumber:int · duckId:int · name:string · weight:int |
| skillLevel | Id:int · skillLevel:int · upCost:float · changeAttr1~3:string · attrValue1~3:float · specialEffect:string |

## 四、UI 统一规范（像素风）

- 背景：`TextureRect + menu_bg.jpg`（禁用 ColorRect）
- 标题：黄色 `(1, 0.85, 0.2)` + 黑色描边 `outline_size=4` + 字号 36
- 按钮：`TextureButton` + `VBoxContainer`（主标签+描述），子节点 `mouse_filter=2`
- 面板：`StyleBoxFlat` 深色背景 `(0.08, 0.1, 0.14, 0.92)` + 边框 + 圆角 8
- 顶部栏：返回按钮(48x48)+BackIcon+标题+Spacer+金币图标(32x32)+金币数
- 资源存 `Textures/ui/`：menu_bg/icon_back/icon_coin/btn_upgrade/btn_shop/btn_equip/btn_start.jpg

## 五、修改历史（倒序）

### 2026-08-19（修复战斗返回备战后按钮失效）
- 问题：UIManager.ui_root 引用已销毁的 GameManager，open_ui 报错返回
- 修复：UIManager.open_ui 检测 ui_root 失效时回退到 get_tree().root
- 原因：change_scene_to_file 销毁 GameManager 场景，但 UIManager(Autoload) 的 ui_root 仍指向旧节点

### 2026-08-19（按钮背景色+hover+战斗返回）
- _add_hover 改进：正常态 modulate=1.15（按钮更显眼）+ hover 态 1.3
- UIBase 新增 _ui_managed 标志 + _ready 自动初始化（非 UIManager 管理时自动 on_create/on_open）
- UIManager.open_ui 设置 _ui_managed=true 避免 _ready 重复初始化
- 所有场景按钮添加 _add_hover：preparation(5)/startui(3)/settings(2)/out_of_battle_upgrade(1+动态)/shop(1+动态)/equipment(1+动态)/save_select(3)/confirm_dialog(2)
- battle_manager.gd 返回备战界面（preparation.tscn）+ BackBtn 改为 TextureButton
- battle.tscn 添加 btn_start.jpg 背景 + BackLabel

### 2026-08-19（修正 UI 布局连带问题）
- settings.tscn：添加顶层 TopBar（标题"设置"），MainContainer 添加 offset_top=60，删除内部 TitleBar
- save_select.tscn：Slot 添加 size_flags_vertical=4（垂直居中），修正 SlotContainer 填充后 Slot 顶部对齐问题
- 修正原因：上次仅改 margin 未调整内部元素，导致标题位置/Slot 垂直对齐错误

### 2026-08-19（统一 UI 尺寸）
- 统一标准：TopBar 高度60 + MainContainer margin 80/20/80/40 + offset_top=60
- 修改场景：settings（margin 200/50→80/20/80/40）、shop/equipment/out_of_battle_upgrade（margin_bottom 20→40）
- save_select 重构：TitleBar 从 MainContainer 内移到顶层 TopBar，同步 .gd 节点路径
- start_ui 保持 CenterContainer 居中布局（主菜单特殊，不适用 TopBar 规范）
- 接口文档 UI 规范新增尺寸标准

### 2026-08-19（备战界面布局重构）
- 新布局：HBoxContainer(MainRow) 左侧 LeftVBox(局外养成+商店) + 右侧 CenterVBox(开始狩猎+装备)
- 开始狩猎按钮居中偏下（TopSpacer 占据上方空间），下方紧跟装备按钮
- StartBattleBtn 重命名为 StartHuntBtn，文本"开始战斗"→"开始狩猎"
- preparation.gd 节点路径同步更新

### 2026-08-19（修复 confirm_dialog 空指针）
- 根因：`@onready` 变量在 `_ready()` 初始化，而 `_ready()` 在 `add_child()` 后才调用
- save_select.gd 的 `_on_slot_delete` 原在 `add_child()` 前调 `setup()`，导致访问 null 的 `@onready` 变量
- 修复：`add_child(dialog)` 移到 `setup()` 之前
- **经验：动态实例化场景时，必须先 `add_child()` 再访问 `@onready` 变量**

### 2026-08-19（代码抽象与重复消除）
- UIBase.gd 新增 4 个公共辅助方法：`_add_hover` / `_refresh_gold_label` / `_create_list_row` / `_clear_container`
- preparation/out_of_battle_upgrade/shop/equipment.gd 改用 UIBase 公共方法，消除 5 处重复代码
- save_select.gd 删除重复的 `_add_hover`，使用 UIBase 的
- 修复 3 处 `texture` → `texture_normal` bug（out_of_battle_upgrade/shop/equipment 的动态创建按钮）
- 接口文档同步更新 UIBase 方法表

### 2026-08-19（创建接口文档）
- 新建 `接口文档.md`：整理所有 Autoload 单例、核心类、UI 场景、数据表结构、目录结构、UI 规范
- **关键规则更新**：修改前必须读「项目进程汇报」+「接口文档」；修改后同步更新两个文件
- 今后每次写代码前都需读取接口文档，确保接口调用正确

### 2026-08-19（可复用确认对话框）
- 新建 confirm_dialog.tscn + confirm_dialog.gd（class_name ConfirmDialog）
- 布局：半透明遮罩 + 深色面板(StyleBoxFlat 黄色边框 圆角8) + 标题 + 描述 + 确认/取消按钮
- API：setup(title, message, confirm_cb, cancel_cb) 一行调用
- save_select.gd 的 _on_slot_delete 从 116 行内联代码 → 14 行调用，可复用

### 2026-08-19（清除隐藏图标 + 恢复按钮纹理）
- 清除 3 个被手动隐藏的 CoinIcon（preparation/out_of_battle_upgrade/shop）
- 移除 icon_coin.jpg ext_resource 引用
- 修复 Godot 重新保存导致的按钮 texture 丢失：恢复 5 个按钮的 btn_start.jpg 引用
- start_ui.tscn 按钮纹理统一为 btn_start.jpg（原 gear.png/power.png/buttonStart.png）
- **经验：Godot 编辑器保存场景时会自动清理"未使用"的 ext_resource，如果按钮 texture 被手动删除，对应的 ext_resource 也会被清理**

### 2026-08-19（按钮图标遮挡修复）
- 4 个场景删除 BackIcon/UpgradeIcon/ShopIcon/EquipIcon 共 7 个图标节点
- 所有按钮统一 btn_start.jpg 背景 + Label 文字标识（返回/局外养成/商店/装备/开始战斗）
- 移除冗余 ext_resource：icon_back.jpg/btn_upgrade.jpg/btn_shop.jpg/btn_equip.jpg

### 2026-08-19（存档选择界面完善）
- 删除不必要节点：BackIcon/TitleSpacer×2/SlotIcon×3/DeleteIcon×3（共 8 个）
- 移除 icon_back.jpg、save.png 引用，load_steps 6→5
- 所有按钮统一文字标识 + btn_start.jpg 背景：返回/继续/创建/删除
- BackBtn：icon_back 翻转 → btn_start.jpg + Label"返回"
- DeleteBtn：icon_back 翻转 → btn_start.jpg + Label"删除"（红色字 `(1, 0.6, 0.6)`）
- 空存档时隐藏 DeleteBtn（`delete_btn.visible = false`）
- BackBtn 也加 hover 效果

### 2026-08-19（设置界面精简）
- 删除 TitleBar 中的 BackBtn（已有底部返回按钮，重复）
- 移除 icon_back.jpg ext_resource 引用，load_steps 7→6
- 恢复默认/返回按钮描边 outline_size 3→5，更显眼

### 2026-08-19（存档选择界面优化）
- 删除确认：`AcceptDialog` → 像素风格自定义确认面板（半透明遮罩 + 深色 Panel + 黄色标题 + 确认删除/返回按钮）
- 删除逻辑：空存档直接 return；确认后 `delete_slot` + `refresh_all_slots`；返回不删除
- hover 选中效果：存档槽 Panel `modulate` 提亮 1.12；按钮提亮 1.15（`_add_hover` 辅助方法）

### 2026-08-19（设置界面优化）
- 背景：`menu_bg.jpg`（杂乱）→ `panel_bg.jpg`（简洁，符合像素风）
- 保存按钮改为返回按钮：所有设置改动即时保存（分辨率/窗口模式补 `_save_immediate()`），无需手动保存
- 删除 `_on_save_pressed`，SaveBtn 信号改连 `_on_back_pressed`
- 修复 `_on_reset_pressed` 逻辑 bug：原 `_load_settings()` 在保存前调用会覆盖 defaults，改为先保存再加载

### 2026-08-19（UI 布局统一调整 + 信号重复连接修复）
- 重写 start_ui/settings/save_select.tscn：统一 menu_bg 背景 + 像素风按钮 + Panel 样式
- **修复信号重复连接**（11 处 ERROR）：`UIBase.gd` 的 `_ready()` 不再调用 `on_create()/on_open()`，改由 `UIManager.open_ui()` 单独负责（根因：子类 on_create 覆盖父类未调 super，_initialized 标志无效）
- 修复整数除法警告：`int(seconds) / 3600` → `int(seconds / 3600.0)`
- **经验：TextureButton 在脚本中设置纹理用 `texture_normal`，不是 `texture`**（.tscn 文件中用 `texture` 是简写，脚本中必须用全名 `texture_normal`，否则报 Invalid assignment 错误）
- MCP 验证零错误

### 2026-08-19（启用导表插件）
- `project.godot` 添加 `[editor_plugins]` 段启用导表插件

### 2026-08-19（项目规范校验与冗余清理）
- 删除冗余：22 个 .translation + 4 个 .csv.import + tmp-images/ + start UI/ + weapon_icon/ + 武器系统结构.txt
- 补齐 4 张表 .tres：Ducks/Skill/generateProbability/skillLevel

### 2026-08-19（备战场景全面修复）
- 创建 preparation/out_of_battle_upgrade/shop/equipment.tscn
- 修复按钮点击无反应（TextureButton 子节点 mouse_filter=2）
- save_select → preparation 改用 UIManager.open_ui
- UIManager 新增 clear_all()

### 2026-08-19（清理与重建 UI 场景）
- 删除 weapon_test.tscn、旧 prefab/、Scripts/weapon/
- 重建 start_ui/settings/save_select/battle.tscn
- 修复 Autoload 路径、Gamemanager extends Node2D

### 2026-08-19（修复场景加载与类型推断）
- 移除场景文件假 uid，让 Godot 自动生成
- UIManager.open_ui() 加 null 检查
- Variant 返回值必须显式类型声明（不能用 `:=`）
- `AudioServer.set_bus_volume_db()` 需传 int 索引（用 `get_bus_index()`）

### 2026-08-19（项目初始化）
- 建立项目结构：core/battle/buff/meta/ui 目录
- 实现导表工具：CSV→.tres，支持重复ID，保留策划备注行
- 实现 ConfigSystem、SaveSystem（Autoload 单例）
- 转换 4 张数据表：Ducks、Skill、generateProbability、skillLevel
