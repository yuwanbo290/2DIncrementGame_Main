# 项目进程汇报

> 用途：记录本项目每次修改，供后续会话快速了解历史、避免重复犯错。
> 约定：每次修改前先读本文件 + `接口文档.md` + `AI开发规范.md`；修改后追加「修改历史」并同步更新相关规则。

---

## 一、关键规则（务必遵守）

- **修改前必须先读本文件 + `接口文档.md` + `AI开发规范.md`**，修改后追加「修改历史」并同步更新接口文档（用户明确要求）
- **不添加与需求无关的冗余文件**（用户明确强调）
- 表结构由用户设定；**只有拿到对应模块的表结构后才开发该模块**
- **数据基础 = `data/` 下 6 张 xlsx 源表**（Ducks / Skill / generateProbability / skillLevel / shop / weapons），禁止擅自新增无源表的表
- 基础设置存 `Resources/Config/base_config.tres`（Godot 资源，非 JSON）
- 存档用 `user://save.json` + `FileAccess`
- 攻击方式 = 按住左键持续射击（`click` 输入动作）
- UI 框架复用 `UIBase.gd` / `UIManager.gd`（不改生命周期）
- **TextureButton 子节点必须 `mouse_filter = 2`**（否则拦截点击）
- **UI 导航必须通过 UIManager**（`open_ui`/`close_ui`），战斗场景除外
- **Godot 4.x**：`SceneTree` 无 `has_node()`，用 `get_tree().root.has_node()`
- **Variant 返回值必须显式类型声明**（`Dictionary.get()`、`JSON.parse_string()` 等），不能用 `:=`

## 二、导表工具（addons/table_exporter）

- 输入 `data/*.xlsx`（推荐）或 `data/*.csv` → 输出 `Resources/Tables/<表名>.tres`
- 入口：编辑器工具栏「导表」按钮 / `项目 > 工具 > 导表：xlsx/csv → .tres`
- 核心逻辑：`addons/table_exporter/table_exporter_core.gd`（`class_name TableExporterCore`）
- **五段式格式**：第1行字段名 / 第2行类型 / 第3行中文备注 / 第4行默认值（后两行导表时跳过，必须保留）/ 第5行起数据
- **xlsx 解析**：纯 GDScript（`ZIPReader` + `XMLParser`），支持 sharedStrings / inlineStr / 布尔，
  并按 `r="B3"` 坐标补齐 xlsx 省略掉的空单元格与空行（Excel 不会为空单元格写 `<c>`）
- **优先级**：同名时 `.xlsx` 优先于 `.csv`；`~$xxx.xlsx` 锁文件自动跳过；`.xls` 报警提示另存为 xlsx
- **CSV 必须 UTF-8**：检测到替换字符 U+FFFD 时直接 `push_error` 跳过该表，不再静默导出乱码
- **可重复 Id**：表行用 `Array[Dictionary]` 保存；查询用 `TableDB.get_all/get_first/rows_of`
- 类型转换：int（空→0）、float（空→0.0）、bool、string（空→""）

## 三、数据表结构（源表位于 data/，全部为 xlsx）

| 表 | 源文件 | 字段（类型） |
|---|---|---|
| Ducks | Ducks.xlsx | duckID:int · duckName:string · healthNum:float · coin:float · moveSpeed:float · texture:string · spriteFrames:string |
| Skill | Skill.xlsx | Id:int · skillName:string · desc:string · previouId:int · maxLevel:int |
| generateProbability | generateProbability.xlsx | waveNumber:int · duckId:int · name:string · weight:int |
| skillLevel | skillLevel.xlsx | Id:int · skillLevel:int · upCost:float · changeAttr1~3:string · attrValue1~3:float · specialEffect:string |
| shop | shop.xlsx | Id:int · itemType:string · refId:int · propName:string · costNum:int · purchaseLimit:int |
| weapons | weapons.xlsx | weaponId:int · weaponName:string · desc:string · atk:float · atkSpeed:float · isDefault:int · texture:string |

## 四、UI 统一规范（像素风）

- 背景：`TextureRect + menu_bg.jpg`（禁用 ColorRect）
- 标题：黄色 `(1, 0.85, 0.2)` + 黑色描边 `outline_size=4` + 字号 36
- 按钮：`TextureButton` + `VBoxContainer`（主标签+描述），子节点 `mouse_filter=2`
- 面板：`StyleBoxFlat` 深色背景 `(0.08, 0.1, 0.14, 0.92)` + 边框 + 圆角 8
- 顶部栏：返回按钮(48x48)+标题+Spacer+金币图标(32x32)+金币数
- 资源存 `Textures/ui/`：menu_bg / panel_bg / btn_plain / icon_coin.jpg

## 五、修改历史（倒序）
### 2026-08-20（战斗核心玩法实现：射击/刷怪/金币/结算，色块占位）

**一、依据**：鸭子射击增量游戏玩法原型.docx（3选1 Buff 与 Boss 用户明确先不做）

**二、实现**
- Scenes/battle.tscn 重写：根 Node2D + World + UI(CanvasLayer)；背景 ColorRect（深蓝池塘色，放 World 下由脚本铺满——Node2D 下锚点无效）
- Scripts/battle/（新增战斗层，规范 6.1）：
  - nemy.gd（Enemy）：鸭子色块占位——圆形 Polygon2D（颜色按 duckID 调色板）+ 名字 Label + 头顶血条（红底绿面 Polygon2D）；属性全从 Ducks 表读（血量/金币/移速）；移动=随机方向漂移，奇数 duckID 加正弦摆动（原型"不同鸭子不同轨迹"的占位实现）；出界移除
  - ullet.gd（Bullet）：黄色小圆子弹，直线飞行，支持弹射（ricochetCount 边界反弹，来自局外技能「弹性子弹」），超时消失
  - player.gd（BattlePlayer）：绿色圆形色块+“玩家”文字，固定底部中央，瞄准线指向鼠标，按住左键持续射击（冷却=1/atkSpeed）
  - attle_manager.gd：战斗编排——读武器（WeaponService.get_equipped_stats）与局外技能加成（skillLevel 表 atk/bulletCount/ricochetCount 按已学等级累加）；按 generateProbability 表加权随机刷鸭（阶段=击杀数/10+1 封顶表内最大 waveNumber，原型 10以内波1/10-20波2/20+波3）；圆形距离碰撞；击杀得金币（Ducks.coin）+击杀数；倒计时 round_time 结束结算（金币落盘+rounds/best_kills 统计）回备战；返回按钮放弃本局
- 删除旧占位 Scripts/manager/battle_manager.gd

**三、验证（无头 + 渲染双通道）**
- 无头逻辑校验全通过：玩家/鸭子/子弹生成、连发击杀链路、阶段换算（0/9杀=1，10杀=2，20杀=3）、伤害构成
- 真实渲染（窗口模式 + sRGB 转换）视觉确认：深蓝背景、HUD（金币/时间/击杀/阶段）、鸭子橙圆+绿血条、玩家绿圆均在正确位置

**四、踩坑记录**
- **Godot 窗口时序**：_ready 时窗口尺寸可能未生效（window_set_size 异步），位置计算必须延迟到第一帧 _process；窗口 resize 用每帧校正比 size_changed 信号可靠（信号可能在连接前触发）
- **canvas_items 逻辑分辨率**：未配置 viewport 尺寸时默认 1152x648，窗口 1920x1080 下内容等比放大 1.667 倍——实体逻辑坐标与物理像素坐标不同，分析渲染图要按比例换算
- **HDR 管线截图**：get_image() 是线性空间，必须 linear_to_srgb() 后再分析；sRGB 转换后颜色变亮，像素扫描条件要放宽（如玩家绿 R 从 76 升到 148）
- **Array[Bullet].filter(lambda)**：泛型数组 filter 有坑（返回 Array 类型不匹配），手动 for 循环重建

### 2026-08-20（按钮纹理溢出修复：Godot 默认 stretch_mode 陷阱）

**一、用户反馈**：settings 场景 ResetBtn 的纹理 2240x629 超级大，快盖过整个设置背景；其他场景按钮同样问题。

**二、根因（比上一轮更深一层）**
- 布局探针（1920x1080 窗口）证明按钮**布局尺寸正常**（ResetBtn 180x56，ignore_texture_size=true 生效）
- 但 stretch_mode 实际值是 **2（STRETCH_KEEP）**：**Godot 4.7 TextureButton 的默认 stretch_mode 是 STRETCH_KEEP（按原始像素绘制），不是 STRETCH_SCALE**
- 上一轮把 stretch_mode = 5 行删掉后，按钮回落到默认 STRETCH_KEEP → 纹理按 2240x692 原始尺寸绘制 → **溢出按钮、盖住界面**
- 动态按钮（_create_text_button）当时显式设了 STRETCH_SCALE，无此问题（但已统一）

**三、修复**
- 9 个场景全部 TextureButton 显式加 stretch_mode = 6（STRETCH_KEEP_ASPECT_COVERED，等比覆盖、不溢出、不变形）
- UIBase._create_text_button 统一为 KEEP_ASPECT_COVERED（删除旧的 STRETCH_SCALE 行，避免重复赋值）
- 渲染验证（1920x1080 真实渲染 + sRGB 转换）：按钮 = 按钮矩形内的绿色底，背景无溢出

**四、经验**
- **Godot 4.7 TextureButton 默认 stretch_mode = STRETCH_KEEP (2)**，纹理按原始像素绘制——这是"纹理超级大"的常见根因。任何按钮都必须显式设置 stretch_mode
- tn_plain.png（2240x692，比例 3.24:1）与按钮比例接近，KEEP_ASPECT_COVERED 可完美贴合

### 2026-08-20（按钮底图素材问题修复：btn_start.jpg -> btn_plain.png）

**一、问题定位（三轮截图 + 像素级分析）**
- 用户反馈"按钮调整后有问题"：按钮渲染成"白边 + 中央绿杠 + 压扁水印文字"
- 根因：tn_start.jpg（2240x1680）实际是**网上按钮素材**：上下 55% 是白色留白（y0-452 / y1204-1680），中央只有 34% 是绿色按钮区（y496-1068），且绿区内带**素材自带的白色水印文字**（多行字符状白块）
- 按钮用 STRETCH_SCALE 拉伸显示全素材 → 白色留白被压进上下边、绿色区变成中央细杠、水印文字被压扁 → 观感极差
- 判断过程：无头布局探针证明按钮布局尺寸正常（640x100 等，ignore_texture_size 生效）→ 问题在**素材本身**而非布局；用 Godot 真实渲染（窗口模式 + RenderingServer.force_draw + linear_to_srgb 转换）对比确认

**二、修复（生成干净素材 + 全量替换）**
- 用 System.Drawing 从 tn_start.jpg 裁出中央绿色渐变区（y452-1144，含渐变过渡），把水印文字与残边像素填为绿带主色（R44 G221 B17）→ 生成 Textures/ui/btn_plain.png（2240x692，纯绿色渐变，无文字无留白）
- 9 个场景（start_ui/settings/save_select/preparation/out_of_battle_upgrade/shop/weapon/confirm_dialog/battle）的 ext_resource 路径全部改为 tn_plain.png；UIBase.BTN_TEXTURE 同步
- 删除旧素材 tn_start.jpg + .import
- **渲染验证**：Godot 真实渲染 start_ui，按钮区域 = 绿色底 + Label 白字，无白边无水印文字

**三、经验**
- **按钮素材不能带上下留白和水印文字**：拉伸显示会变成"白边+绿杠"，等比显示会露出素材自带文字（双重文字）
- **HDR 管线（Forward+）下 viewport 截图是线性空间**：get_image() 保存前必须 linear_to_srgb()，否则绿色显示成黑色、颜色对比全部失真
- **截图分析的正确路径**：像素网格扫描（System.Drawing）→ 特征色定位 → 渲染探针对照，在视觉模型限流时依然可以推进
- 文档已同步：AI开发规范（素材说明 + 尺寸修正）、接口文档、本文件


### 2026-08-20（全项目按钮显示修正 + 源表全面迁移 xlsx）

**一、全项目 TextureButton 底图修正（20 个按钮，7 个场景）**
- 根因：`.tscn` 里全部写成 `texture = ExtResource(...)`，但 **TextureButton 没有 `texture` 属性**，被 Godot 静默丢弃 → 像素风底图从未渲染过
- 次生问题：底图 `btn_plain.png` 为 **2240×692**，正确设上 `texture_normal` 后若不加 `ignore_texture_size = true`，按钮最小尺寸会被撑到原图大小、直接破坏布局
- 统一改法：`texture` → `texture_normal`，删除无效的 `stretch_mode = 5`（keep-aspect 会让底图在按钮里缩成一小块），补 `ignore_texture_size = true`，尺寸由 `custom_minimum_size` 决定、`stretch_mode` 用默认 `STRETCH_SCALE` 铺满
- 逐场景：`start_ui` 3 个 · `settings` 2 个 · `save_select` 7 个 · `preparation` 5 个 · `out_of_battle_upgrade` 1 个 · `confirm_dialog` 2 个 · `battle` 1 个
- `save_select` / `settings` 原本**连按钮纹理资源都没声明**，已补 `btn_plain.png` 的 ext_resource
- `out_of_battle_upgrade.gd` 动态按钮改用 `UIBase._create_text_button()`（原来手搓且缺 `ignore_texture_size`，4 个升级按钮各会撑到 2240×1680），同时把硬编码颜色提为常量、补 Panel 子节点铺满
- 验证：无头遍历全项目 **34 个 TextureButton（含 8 个动态生成）全部通过**，最小尺寸均正常（如 110×48 / 130×50 / 360×100），异常 0

**二、源表全面迁移到 xlsx（用户要求，便于配置）**
- 剩余 4 张 CSV（Ducks / Skill / skillLevel / generateProbability）用 Excel/WPS COM 转为 `.xlsx`，删除 CSV 与 `.csv.import`
- `data/` 现为纯 xlsx（6 张表），Godot 不识别该扩展名 → 不再产生任何 `.translation` / `.import` 冗余
- **回归验证**：转换后重新导表，与原 CSV 导出的 `.tres` 逐表比对（忽略 ext_resource 随机 id），**6 张表数据零漂移**
- 导表器 CSV 通道仍保留（含 UTF-8 校验拦截），以防将来临时用 csv

**三、文档修正**
- `AI开发规范.md`：修正 8.2 / 14.3 关于「`texture` 是 .tscn 简写」的**错误表述**；导表流程改为 xlsx 优先；新增踩坑 13~16（CSV GBK 静默损坏、xlsx 省略空单元格、TextureButton 双条件、Panel 非容器需铺满）；整改清单勾掉已完成项
- `接口文档.md` / `项目进程汇报.md`：源表格式与表结构同步为 xlsx

**四、玩法原型已读，缺失配置表已列出（等待用户提供后再开发）**
- 读取 `鸭子射击增量游戏玩法原型.docx`，核对现有 6 张表后发现战斗/Buff 相关配置结构缺失，且原型内多处标注「留白：待补充」
- 按用户要求**未擅自造表**，缺口清单见交付说明

### 2026-08-20（商店 / 武器模块实现 + 导表器支持 xlsx）

**一、修复：shop / weapons 源表中文不可逆损坏（阻塞级）**
- 现象：`data/shop.csv`、`data/weapons.csv` 为 **GBK 编码**，导表器用 `FileAccess.get_as_text()` 按 UTF-8 读，
  `手枪`/`步枪` 均被解成 `U+FFFD U+FFFD ǹ`（两行损坏后**完全相同**），乱码已写入 `.tres`
- 处理：按用户决定改用 xlsx——用 Excel/WPS COM 生成 `data/shop.xlsx`、`data/weapons.xlsx`（内容与新表结构一致），删除两个 GBK 源 CSV
- **经验：中文 Windows 上 Excel 另存 CSV 默认是 GBK，导表器按 UTF-8 读会静默产出乱码表；xlsx 内部是 UTF-8 XML，无此问题**

**二、导表器改造：支持 xlsx（用户提议，从根上绕开 CSV 编码问题）**
- `addons/table_exporter/csv_exporter.gd` → **重命名** `table_exporter_core.gd`（`class_name CsvExporter` → `TableExporterCore`），
  同步改 `table_exporter.gd` 的 preload 与菜单名、`plugin.cfg` 描述与版本（1.0.0 → 1.1.0）
- xlsx 解析走纯 GDScript `ZIPReader` + `XMLParser`，无第三方依赖；支持 sharedStrings / inlineStr / 布尔 / 富文本 `<si><r><t>`，跳过 `<rPh>` 注音
- **关键坑：xlsx 不会为空单元格写 `<c>` 节点**（实测生成的 shop 表里 D4 空 propName 整个被省略），
  因此必须按 `r="B3"` 坐标换算列号补齐空列，行同理按 `r` 补齐空行，否则字段整体错位
- CSV 通道保留（4 张老表仍是 csv），但加 UTF-8 校验：检出 U+FFFD 即 `push_error` 跳过，不再静默导乱码
- 同名 xlsx 优先于 csv；跳过 `~$xxx.xlsx` 锁文件；`.xls` 提示另存为 xlsx
- 顺带根治**踩坑 11**：`data/*.csv.import` 的 `importer` 改为 `keep`，清掉 26 个 `.translation` 冗余，实测不再复发（xlsx 因扩展名不被识别，本身不产生 `.import`）

**三、表结构调整（经用户确认）**
- `shop`：`Id/propName/costNum` → `Id/itemType/refId/propName/costNum/purchaseLimit`
  - 原表**没有任何字段关联到 weapons**，只能靠名字字符串匹配，改名即断链 → 加 `refId` 指向 `weapons.weaponId`、`itemType` 区分商品类型
  - 加 `purchaseLimit`（0=不限次 / 1=仅一次）；`costNum` 由 float 改 int（金币是整数）
  - 不加 `desc`：商品描述统一取关联武器的 `desc`，避免同一文案在两张表重复维护
- `weapons`：`weaponId/weaponName/atk/atkSpeed/texture` → 追加 `desc`、`isDefault`
  - `isDefault=1` 表示开局默认拥有（否则玩家 0 金币 0 武器无法开局，而硬编码「id=1 默认」违反规范十一章禁止魔法数字）
- 用户选择「武器界面暂不显示图标」，`texture` 字段保留但代码不读取

**四、存档扩展（向后兼容，SAVE_VERSION 仍为 2）**
- 存档槽新增 `owned_weapons: Array`（已购武器 id）、`equipped_weapon: int`（装备武器 id）
- `_empty_slot()` 补默认值 + `load_save()` 补全缺失字段（老档自动补齐，实测老档中残留的 `meta_upgrades`/`weapon_id` 不影响读取）
- 新增 `get_owned_weapons` / `add_owned_weapon` / `get_equipped_weapon` / `set_equipped_weapon`（均不落盘，调用方负责 save）
- 字段为纯新增、不破坏结构，故未递增 SAVE_VERSION、无需迁移函数

**五、新增核心层 WeaponService（`Scripts/core/weapon_service.gd`）**
- 拥有判定 = 表内 `isDefault=1` ∪ 存档 `owned_weapons`：**默认武器由表决定、不写存档**，改表即生效且无需存档迁移
- `get_equipped_id()` 在存档值缺失/失效时回退首个默认武器，保证任何存档都有可用武器
- 商店与武器界面共用拥有/装备判定，避免两处重复实现；`get_equipped_stats()` 为未来战斗层预留读属性入口

**六、商店 / 武器界面（从空占位改为实现）**
- `shop`：列出 shop 表商品（名称 + 关联武器属性/DPS + 价格 + 购买按钮）；状态分「购买 / 已拥有 / 金币不足 / 配置缺失 / 暂不支持」，点击时**二次校验**防列表状态过期；成功后扣金币 + 记录武器 + 一次落盘
- `weapon`：列出全部武器（名称 + 描述 + 攻击/射速/每秒伤害 + 装备按钮）；已装备行用绿色边框区分，顶栏显示当前武器；未拥有置灰提示「商店购买」
- UIBase 新增两个共享原语 `_create_text_button` / `_create_empty_hint` + 常量 `BTN_TEXTURE`（规范 8.3 禁止重复造轮子）

**七、发现但未修复的全项目级 Bug（需用户拍板）**
- 全项目 TextureButton 在 .tscn 里写的是 `texture = ExtResource(...)`，但 **TextureButton 没有 `texture` 属性**，被 Godot 静默丢弃
- 无头实测：preparation / start_ui / out_of_battle_upgrade / shop 等 10 个 TextureButton 的 `texture_normal` **全部为 null**，即像素风按钮底图从未真正渲染
- 且 `btn_plain.png` 是 **2240x692**，一旦正确设上 `texture_normal` 却不设 `ignore_texture_size = true`，按钮最小尺寸会变成 2240x1680 直接撑爆布局
  （`out_of_battle_upgrade.gd` 动态按钮已正确用 `texture_normal` 但缺 `ignore_texture_size`，属此隐患）
- 本次只在**新写的** shop / weapon 与 UIBase 新原语中用了正确写法（`texture_normal` + `ignore_texture_size = true` + `STRETCH_SCALE`），未擅自改动其余场景
- **`AI开发规范.md` 8.2 / 14.3 关于「.tscn 用 texture 简写」的表述是错的，需用户确认后修正**

**八、验证方式**
- 无头导表：6/6 表成功，shop/weapons 走 xlsx，`.tres` 内 `手枪`/`步枪` 与新字段全部正确
- 无头功能校验 39 项全通过：表数据、存档字段补全、默认武器、金币不足拒绝购买、扣款、限购不可重复、装备/未拥有拒绝装备、按钮纹理与 `ignore_texture_size`
- 校验脚本为临时文件，验证后已全部删除；沙箱阻止了 `user://` 写入，用户真实存档 SHA256 前后一致、未被改动

### 2026-08-19（备战界面：恢复商店/局外养成/武器入口）
- preparation 备战界面加回三个按钮：局外养成 / 商店 / 武器，并补全按钮 btn_plain.png 纹理与 mouse_filter
- 局外养成（out_of_battle_upgrade）：以 `Skill` 表为数据源展示技能并支持金币升级；升级消耗来自 `skillLevel` 表 upCost，前置技能（previouId）需 ≥1 级解锁，满级禁用
- 商店（shop）/ 武器（weapon）：空占位界面，显示"开发中，等待数据"
- SaveSystem 存档槽加 `skill_levels` 字段 + `get/set_skill_level` 方法（技能等级落盘，向后兼容补全）
- 同步更新接口文档 / 进程汇报 / AI开发规范

### 2026-08-19（回归数据基础：清理无 CSV 表及其派生代码）
- 数据基础回归 `data/` 4 张 CSV，删除无 CSV 源的两张表：`weapon_table`、`upgrade_table`（.tres）
- 删除依赖这两张表的 UI：shop（商店）/ equipment（装备）/ out_of_battle_upgrade（局外养成）场景+脚本
- preparation 备战界面精简：移除局外养成/商店/装备入口，只保留「返回 + 开始狩猎」
- SaveSystem 存档槽精简：删除 `weapon_id`/`owned_weapons`/`meta_upgrades` 字段及 `get/set_upgrade_level` 方法，只保留金币 + 统计
- 清理冗余：`data/` 下 22 个 .translation + 4 个 .csv.import（CSV 误导入产物）；`Textures/ui` 下 btn_shop/btn_upgrade/btn_equip/icon_back（无引用）
- 新建 `AI开发规范.md`（开发准则），并精简三份文档（接口文档 / 进程汇报 / AI开发规范）

### 2026-08-19（修复战斗返回备战后按钮失效）
- 问题：UIManager.ui_root 引用已销毁的 GameManager，open_ui 报错返回
- 修复：UIManager.open_ui 检测 ui_root 失效时回退到 get_tree().root
- 原因：change_scene_to_file 销毁 GameManager 场景，但 UIManager(Autoload) 的 ui_root 仍指向旧节点

### 2026-08-19（按钮背景色+hover+战斗返回）
- _add_hover 改进：正常态 modulate=1.15（按钮更显眼）+ hover 态 1.3
- UIBase 新增 _ui_managed 标志 + _ready 自动初始化（非 UIManager 管理时自动 on_create/on_open）
- UIManager.open_ui 设置 _ui_managed=true 避免 _ready 重复初始化
- battle_manager.gd 返回备战界面（preparation.tscn）+ BackBtn 改为 TextureButton
- battle.tscn 添加 btn_plain.png 背景 + BackLabel

### 2026-08-19（修正 UI 布局连带问题）
- settings.tscn：添加顶层 TopBar（标题"设置"），MainContainer 添加 offset_top=60，删除内部 TitleBar
- save_select.tscn：Slot 添加 size_flags_vertical=4（垂直居中），修正 SlotContainer 填充后 Slot 顶部对齐问题

### 2026-08-19（统一 UI 尺寸）
- 统一标准：TopBar 高度60 + MainContainer margin 80/20/80/40 + offset_top=60
- save_select 重构：TitleBar 从 MainContainer 内移到顶层 TopBar，同步 .gd 节点路径
- start_ui 保持 CenterContainer 居中布局（主菜单特殊，不适用 TopBar 规范）

### 2026-08-19（备战界面布局重构）
- 新布局：HBoxContainer(MainRow) 左侧 LeftVBox(局外养成+商店) + 右侧 CenterVBox(开始狩猎+装备)
- StartBattleBtn 重命名为 StartHuntBtn，文本"开始战斗"→"开始狩猎"

### 2026-08-19（修复 confirm_dialog 空指针）
- 根因：`@onready` 变量在 `_ready()` 初始化，而 `_ready()` 在 `add_child()` 后才调用
- 修复：`add_child(dialog)` 移到 `setup()` 之前
- **经验：动态实例化场景时，必须先 `add_child()` 再访问 `@onready` 变量**

### 2026-08-19（代码抽象与重复消除）
- UIBase.gd 新增 4 个公共辅助方法：`_add_hover` / `_refresh_gold_label` / `_create_list_row` / `_clear_container`
- save_select.gd 删除重复的 `_add_hover`，使用 UIBase 的
- 修复 3 处 `texture` → `texture_normal` bug（动态创建按钮）
- 接口文档同步更新 UIBase 方法表

### 2026-08-19（创建接口文档）
- 新建 `接口文档.md`：整理所有 Autoload 单例、核心类、UI 场景、数据表结构、目录结构、UI 规范
- **关键规则更新**：修改前必须读「项目进程汇报」+「接口文档」；修改后同步更新两个文件

### 2026-08-19（可复用确认对话框）
- 新建 confirm_dialog.tscn + confirm_dialog.gd（class_name ConfirmDialog）
- API：setup(title, message, confirm_cb, cancel_cb) 一行调用
- save_select.gd 的 _on_slot_delete 从 116 行内联代码 → 14 行调用，可复用

### 2026-08-19（清除隐藏图标 + 恢复按钮纹理）
- 修复 Godot 重新保存导致的按钮 texture 丢失：恢复按钮的 btn_plain.png 引用
- **经验：Godot 编辑器保存场景时会自动清理"未使用"的 ext_resource**

### 2026-08-19（按钮图标遮挡修复）
- 所有按钮统一 btn_plain.png 背景 + Label 文字标识
- 移除冗余 ext_resource：icon_back.jpg/btn_upgrade.jpg/btn_shop.jpg/btn_equip.jpg

### 2026-08-19（存档选择界面完善）
- 所有按钮统一文字标识 + btn_plain.png 背景：返回/继续/创建/删除
- 空存档时隐藏 DeleteBtn（`delete_btn.visible = false`）

### 2026-08-19（设置界面精简）
- 删除 TitleBar 中的 BackBtn（已有底部返回按钮，重复）
- 恢复默认/返回按钮描边 outline_size 3→5，更显眼

### 2026-08-19（存档选择界面优化）
- 删除确认：`AcceptDialog` → 像素风格自定义确认面板
- 删除逻辑：空存档直接 return；确认后 `delete_slot` + `refresh_all_slots`
- hover 选中效果：存档槽 Panel `modulate` 提亮 1.12

### 2026-08-19（设置界面优化）
- 背景：`menu_bg.jpg`（杂乱）→ `panel_bg.jpg`（简洁，符合像素风）
- 保存按钮改为返回按钮：所有设置改动即时保存
- 修复 `_on_reset_pressed` 逻辑 bug：先保存默认值再加载

### 2026-08-19（UI 布局统一调整 + 信号重复连接修复）
- 重写 start_ui/settings/save_select.tscn：统一 menu_bg 背景 + 像素风按钮 + Panel 样式
- **修复信号重复连接**（11 处 ERROR）：`UIBase.gd` 的 `_ready()` 不再调用 `on_create()/on_open()`，改由 `UIManager.open_ui()` 单独负责
- 修复整数除法警告：`int(seconds) / 3600` → `int(seconds / 3600.0)`
- **经验：TextureButton 在脚本中设置纹理用 `texture_normal`，不是 `texture`**

### 2026-08-19（启用导表插件）
- `project.godot` 添加 `[editor_plugins]` 段启用导表插件

### 2026-08-19（项目规范校验与冗余清理）
- 删除冗余：22 个 .translation + 4 个 .csv.import + tmp-images/ + start UI/ + weapon_icon/ + 武器系统结构.txt
- 补齐 4 张表 .tres：Ducks/Skill/generateProbability/skillLevel

### 2026-08-19（备战场景全面修复）
- 创建 preparation/out_of_battle_upgrade/shop/equipment.tscn
- 修复按钮点击无反应（TextureButton 子节点 mouse_filter=2）
- UIManager 新增 clear_all()

### 2026-08-19（清理与重建 UI 场景）
- 删除 weapon_test.tscn、旧 prefab/、Scripts/weapon/
- 重建 start_ui/settings/save_select/battle.tscn
- 修复 Autoload 路径、Gamemanager extends Node2D

### 2026-08-19（修复场景加载与类型推断）
- 移除场景文件假 uid，让 Godot 自动生成
- UIManager.open_ui() 加 null 检查
- Variant 返回值必须显式类型声明（不能用 `:=`）
- `AudioServer.set_bus_volume_db()` 需传 int 索引（用 `get_bus_index()`）

### 2026-08-19（项目初始化）
- 建立项目结构：core/battle/buff/meta/ui 目录
- 实现导表工具：CSV→.tres，支持重复ID，保留策划备注行
- 实现 ConfigSystem、SaveSystem（Autoload 单例）
- 转换 4 张数据表：Ducks、Skill、generateProbability、skillLevel
