# 项目进程汇报

> 用途：记录本项目每次修改，供后续会话快速了解历史、避免重复犯错。
> 约定：修改前必读本文件 + `接口文档.md` + `AI开发规范.md`；修改后追加「修改历史」并同步更新接口文档。
> **精简规则（强制）**：本文件一律精简——修改历史每条 ≤ 2 行，规则/说明每条一行一事，禁长篇复述；细节交给 `AI开发规范.md` / `接口文档.md`。

## 一、关键规则（务必遵守）

- 修改前必读本文件 + `接口文档.md` + `AI开发规范.md`；修改后追加「修改历史」并同步更新接口文档
- 不添加与需求无关的冗余文件（用户明确强调）
- 表结构由用户设定；只有拿到对应模块的表结构后才开发该模块
- 数据基础 = `data/` 下 8 张 xlsx 源表，禁止擅自新增无源表的表
- 基础设置存 `Resources/Config/base_config.tres`（Godot 资源，非 JSON）
- 存档用 `user://save.json` + `FileAccess`；攻击方式 = 按住左键持续射击（`click`）
- UI 复用 `UIBase.gd` / `UIManager.gd`（不改生命周期）；导航必须走 UIManager（战斗场景除外）
- TextureButton 子节点必须 `mouse_filter = 2`；底图用 `texture_normal` + `ignore_texture_size = true`
- Godot 4.x：用 `get_tree().root.has_node()`；Variant 返回值必须显式类型声明（禁 `:=`）

## 二、关键信息速览

- 引擎：Godot 4.7（Forward Plus / d3d12），GDScript（禁 C# / GDExtension）
- 导表：`data/*.xlsx`（五段式）→ `Resources/Tables/*.tres`（编辑器菜单「导表」）；查询用 `TableDB`
- 数据表 8 张：Enemy / Skill / generateProbability / skillLevel / shop / weapons / Buff / buffLevel（字段见 `接口文档.md`）
- UI 像素风规范、踩坑清单、

## 三、修改历史（倒序，精简版）

### 2026-08-25 数值系统平滑曲线调优（仅改数值/权重，未动结构）
- 节奏：spawn_interval 2.0→0.6、spawn_per_wave 1→2（每秒~3.3怪），waveBoss CreateCost 10~25→6~15，60 秒可全通 7 波；经验曲线改 exp=(4,1.3,5)：前 3 波每波升 1 级后放缓，单局升 5~6 级
- 敌人：普通血 5~80→6~100、Boss 血 50~350→40~180（匹配逐波 DPS）；金币上调（Boss 15~90）单局 300~500
- 经济：技能费用等比 ~1.5x/级（基础 100 起、迅捷施法 50 起），引兽香 -0.05~-0.2（防 spawn_interval 变负）、嗜血吸引 +1~+3
- Buff 权重：火力 100→60、明镜 10→25、其余 20~45（有层次不垄断）；base_attack 由 2.0 修正为 10.0（base_config.tres 残留调试值）；备份 xlsx 于 _数值备份_20260825/

### 2026-08-25 局外养成改左右布局 + 技能树可点 + Buff 描述只读配置文本
- out_of_battle_upgrade：左侧技能树节点改为可点击 Button（选中金色高亮），右侧详情面板读取 Skill/skillLevel 表文本（名称/描述/等级/前置/下一级效果 desc/费用/升级按钮）；tscn 改 HBox 布局；skillLevel 表已含 changeAttr1~4+desc；buff 卡面描述只显示 buffLevel.desc（去掉 Buff 表 desc 与自动拼接）

### 2026-08-25 接入 Buff/buffLevel 新表数据（12 Buff + 4 属性槽）
- Buff 表扩为 12 个（新增暴击率/暴击伤害/攻速类），buffLevel 改为 changeAttr1~4 + 每级 desc：ATTRIBUTE_SLOT_COUNT 3→4；卡面效果文案优先用 buffLevel.desc；_describe_buff_level 补 base_config 字段名友好文案；新增 _get_fire_interval/_refresh_fire_interval 使攻速 Buff 即时生效；同步调试指令对照表/接口文档/AI规范

### 2026-08-24 局外养成改为传统技能树
- out_of_battle_upgrade 从平铺列表改为技能树：按 Skill.previouId 分层（0/缺省=根），BFS 深度、同层均分居中；节点面板（名称/描述/等级/升级按钮，锁定/满级/金币不足状态）+ 新 skill_tree_canvas.gd 绘制前置连线（解锁亮绿）；tscn 的 SkillList 改为 TreeCanvas

### 2026-08-24 Boss 机制调整：不清场、Boss 存活期间照常刷怪
- 按用户要求：击杀 Boss 不再清掉场上小怪（保留直接进下一波）；移除 Boss 在场时冻结刷怪逻辑（Boss 存活期间照常生成小怪）；同步 AI规范 10.5

### 2026-08-24 引入 Boss 机制：waveBoss 表驱动波次推进
- 新增 waveBoss 表（waveNumber/enemyId/name/CreateCost/weight，用户已导表）：本波击杀数达 CreateCost 刷新 Boss（文本提示），击杀 Boss 清场推进下一波（提示），最后一波打完直接结算；Enemy 表已含 Boss 7~13；battle.tscn 新增居中 NoticeLabel；删 base_config kills_per_stage/boss_nodes（被 waveBoss 取代）；同步调试指令/接口文档/AI规范

### 2026-08-24 skillLevel 属性关联 base_config 7 字段
- skillLevel.changeAttr 可直接写 base_config 字段名（base_attack/base_attack_speed/base_crit_rate/base_crit_dmg/round_time/spawn_interval/spawn_per_wave），战斗开始备份并累加、退出恢复；_load_meta_bonuses 提前到读取本局节奏前；用法写入调试指令.md

### 2026-08-24 局内 Buff 改经验制：击杀得经验，攒满升级触发三选一
- 击杀按 Enemy.exp 获得局内经验，Exp(level)=exp_base×level^exp_power+exp_linear×(level-1) 满即升级扣经验弹 Buff；删 base_config.buff_trigger_kills 击杀节点机制；battle.tscn 底部新增经验条（ProgressBar+等级/数值）；同步调试指令/接口文档/AI规范

### 2026-08-24 generateProbability 表外键改名：goblinId → enemyId
- generateProbability 表 goblinId→enemyId（用户已导表）；battle_manager 刷怪取字段同步；同步接口文档/AI规范

### 2026-08-24 哥布林表改名：Goblins → Enemy（enemyID + exp）
- 源表/导出表 Goblins→Enemy、主键 goblinID→enemyID（用户已导表）；battle_manager TABLE_GOBLINS→TABLE_ENEMY + 查询字段，enemy.gd 变量/注释同步；新增 exp 列暂不处理；删孤儿 Goblins.tres；同步接口文档

### 2026-08-24 新增调试系统（DebugSystem）
- Autoload DebugSystem：反引号开关控制台；gold 加金币 / config 改 base_config（运行时）/ buff 战斗中加 Buff；仅编辑器生效（OS.has_feature("editor")）发布包禁用；指令文档 = 根目录 调试指令.md

### 2026-08-24 Buff 新属性 burstCount（一次射击连射轮数）
- 新增属性 key `burstCount`（每射击额外轮数，实际轮数=1+值）：_apply_attribute_change 支持、连射轮数文案；_on_player_fire 重构为 _fire_burst_round 多轮顺次发射（轮间 0.08s）；未改任何配置表，buffLevel 表由用户自填

### 2026-08-24 战斗倒计时显眼化 + 结算界面
- battle.tscn 加大号居中倒计时（46px，最后 10 秒红闪放大），_process 每帧刷新 HUD；新增 battle_result 结算弹层（本局伤害/击杀/金币/阶段），_finish_round 先展示结算暂停、确认后才落盘回备战

### 2026-08-24 战斗反馈优化：受击血条 + 伤害数字
- enemy 血条默认隐藏仅受击显示；新增 DamageNumber（命中飘字，暴击橙色大字「暴击」）；Bullet 增加 is_crit 标记

### 2026-08-24 玩家基础数值接入 base_config（攻击叠加/攻速/暴击）
- battle_manager 接入 base_attack/base_attack_speed/base_crit_rate/base_crit_dmg：伤害=base_attack+武器atk+加成；攻速=base_attack_speed×武器atkSpeed；每发独立暴击判定；空手兜底 atk=0；顺手修 _stage_from_kills 整数除法警告

### 2026-08-23 开发环境：接入 Godot MCP 服务器
- Cline MCP 注册 `godot`（`npx.cmd @coding-solo/godot-mcp` + `GODOT_PATH` 指向 Steam 版 Godot）；配置写入 `~/.cline/data/settings/cline_mcp_settings.json`（Cline 4.x 默认路径，旧版 globalStorage 路径不再读取）
- 关键坑：Windows 上 Cline 用 spawn 启动 MCP 服务器，`npx` 报 ENOENT 必须用 `npx.cmd`

### 2026-08-23 战斗数值配置化：battle_manager 常量 → base_config.tres
- 4 个数值常量（KILLS_PER_STAGE / BULLET_SPEED / SPREAD_HALF_ANGLE / MUZZLE_OFFSET）迁入 `base_config.tres`，改用 `config.xxx` 调用；表名 / ATTRIBUTE_SLOT_COUNT 等技术常量保留在代码

### 2026-08-23 整体主题迁移：鸭子 → 哥布林
- 源表/运行表 Ducks→Goblins；品牌改 GOBLIN RUSH/哥布林来袭；UI 换哥布林素材；存档无表名字段，无需迁移

### 2026-08-20 战斗核心玩法实现（射击/刷怪/金币/结算，色块占位）
- battle.tscn + Scripts/battle（enemy/bullet/player/battle_manager）：加权刷怪、连发射击、击杀得金币、倒计时结算回备战；无头+渲染双通道验证

### 2026-08-20 按钮纹理溢出修复
- 根因：Godot 4.7 TextureButton 默认 `stretch_mode=STRETCH_KEEP` 按原始像素绘制 → 9 场景全部显式 `stretch_mode=6`

### 2026-08-20 按钮底图素材修复（btn_start.jpg → btn_plain.png）
- btn_start.jpg 带上下留白+水印，用 System.Drawing 裁绿区生成 btn_plain.png（2240×692），9 场景替换

### 2026-08-20 全项目按钮显示修正 + 源表迁移 xlsx
- 20 个按钮 `texture`→`texture_normal`+`ignore_texture_size=true`；4 张 CSV→xlsx，data/ 纯 xlsx 无编码冗余

### 2026-08-20 商店/武器模块实现 + 导表器支持 xlsx
- GBK 乱码→xlsx 根治；导表器 CsvExporter→TableExporterCore（ZIPReader+XMLParser）；shop/weapons 表扩展（refId/purchaseLimit/isDefault）；存档加 owned_weapons/equipped_weapon（向后兼容）；WeaponService 默认武器由 isDefault 决定

### 2026-08-19 备战界面：恢复商店/局外养成/武器入口
- preparation 加回三入口；局外养成按 Skill/skillLevel 实现金币升级（前置解锁/满级禁用）；存档加 skill_levels

### 2026-08-19 回归数据基础：清理无 CSV 表及派生代码
- 删 weapon_table/upgrade_table 与商店/装备/局外养成 UI；存档精简为金币+统计；新建 `AI开发规范.md`

### 2026-08-19 修复战斗返回备战后按钮失效
- 根因：change_scene 销毁旧场景后 UIManager.ui_root 失效 → open_ui 回退 get_tree().root

### 2026-08-19 按钮背景色+hover+战斗返回
- _add_hover 正常 1.15/hover 1.3；UIBase._ui_managed 避免 _ready 重复初始化；battle 返回备战

### 2026-08-19 修正 UI 布局连带问题
- settings 顶层 TopBar + offset_top=60；save_select Slot 垂直居中

### 2026-08-19 统一 UI 尺寸
- 标准：TopBar 高 60 + MainContainer margin 80/20/80/40 + offset_top=60；save_select TitleBar 上移

### 2026-08-19 备战界面布局重构
- HBox 分栏（养成/商店 | 开始狩猎/装备）；StartBattleBtn→StartHuntBtn

### 2026-08-19 修复 confirm_dialog 空指针
- 经验：动态实例化场景必须先 add_child() 再访问 @onready

### 2026-08-19 代码抽象与重复消除
- UIBase 新增 _add_hover/_refresh_gold_label/_create_list_row/_clear_container；save_select 复用

### 2026-08-19 创建接口文档
- 新建 `接口文档.md`；约定修改前后必读/同步更新

### 2026-08-19 可复用确认对话框
- confirm_dialog.tscn + ConfirmDialog.setup() 一行调用

### 2026-08-19 按钮纹理/图标清理
- 恢复被编辑器清理的 btn_plain 引用；删冗余 icon_back/btn_upgrade/btn_shop/btn_equip；按钮统一底图+Label 文字

### 2026-08-19 存档选择界面完善
- 按钮统一文字标识；空档隐藏删除按钮；AcceptDialog→自定义确认面板；hover 提亮

### 2026-08-19 设置界面优化
- 删重复 BackBtn；panel_bg 背景；即时保存；修复 reset 先存后载 bug

### 2026-08-19 UI 布局统一 + 信号重复连接修复
- 重写 start_ui/settings/save_select；UIBase._ready 不再调 on_create/on_open（改由 UIManager.open_ui 负责，修 11 处 ERROR）；整数除法修复

### 2026-08-19 启用导表插件
- project.godot 添加 [editor_plugins]

### 2026-08-19 项目规范校验与冗余清理
- 删 22 个 .translation + 4 个 .csv.import + tmp-images/ 等；补 4 张表 .tres

### 2026-08-19 备战场景全面修复
- 建 preparation/out_of_battle_upgrade/shop/equipment.tscn；修 TextureButton 点击无反应（mouse_filter=2）；UIManager.clear_all()

### 2026-08-19 清理与重建 UI 场景
- 删旧占位与 prefab；重建 start_ui/settings/save_select/battle.tscn；修 Autoload 路径

### 2026-08-19 修复场景加载与类型推断
- 移除假 uid；open_ui 加 null 检查；Variant 显式类型；AudioServer 传 int 索引

### 2026-08-19 项目初始化
- 建 core/battle/buff/meta/ui 结构；导表工具 CSV→.tres；ConfigSystem/SaveSystem；转换 4 张表
