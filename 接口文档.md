# 接口文档

> 用途：整理本项目所有代码的公共接口，供后续开发参考。
> 约定：每次写代码前必须先读本文件 + 项目进程汇报，确保接口调用正确。

---

## 一、Autoload 单例（全局可访问）

注册于 `project.godot` 的 `[autoload]` 段，可在任意脚本中直接通过单例名访问。

### 1.1 UIManager（UI 管理器）

- **继承**：`Node`
- **路径**：`Scripts/manager/UIManager.gd`
- **职责**：管理所有 UI 场景的创建、打开、关闭、销毁，带缓存

| 方法签名 | 说明 |
|---|---|
| `func initialize(root: Node)` | 初始化 UI 根节点（由 GameManager 调用） |
| `func open_ui(ui_name: String, path: String)` | 打开/显示 UI（首次加载实例化并缓存，后续调 on_open） |
| `func close_ui(ui_name: String)` | 隐藏 UI（调 on_close，不销毁） |
| `func destroy_ui(ui_name: String)` | 销毁 UI（调 on_destroy，移出缓存） |
| `func clear_all()` | 清空所有缓存 UI（切场景前调用） |

**使用示例**：
```gdscript
UIManager.open_ui("save_select", "res://Scenes/ui/save_select.tscn")
UIManager.close_ui("start_ui")
```

### 1.2 ConfigSystem（基础配置）

- **继承**：`Node`
- **路径**：`Scripts/core/config_system.gd`
- **职责**：启动时加载 `res://Resources/Config/base_config.tres`

| 属性/方法 | 类型 | 说明 |
|---|---|---|
| `var config: BaseConfig` | `BaseConfig` | 基础配置实例，只读访问 |

**使用示例**：
```gdscript
var atk: float = ConfigSystem.config.base_attack
var interval: float = ConfigSystem.config.spawn_interval
```

### 1.3 SaveSystem（存档系统）

- **继承**：`Node`
- **路径**：`Scripts/core/save_system.gd`
- **职责**：管理 `user://save.json`，支持 3 个独立存档槽

#### 常量
| 常量 | 值 | 说明 |
|---|---|---|
| `SAVE_PATH` | `"user://save.json"` | 存档文件路径 |
| `SAVE_VERSION` | `2` | 存档版本号 |
| `SLOT_COUNT` | `3` | 存档槽数量 |

#### 存档槽结构（Dictionary）
```gdscript
{
    "id": int,              # 槽位序号 0/1/2
    "name": String,         # 存档名（空字符串=空槽）
    "created_at": int,      # 创建时间戳
    "last_played": int,     # 上次游玩时间戳
    "playtime": float,      # 累计游玩秒数
    "gold": int,            # 金币
    "meta_upgrades": Dictionary,  # 局外养成等级 {upgrade_id: level}
    "stats": Dictionary,    # 统计 {rounds: int, best_kills: int}
    "weapon_id": String,    # 当前出战武器ID
    "owned_weapons": Array, # 已拥有的武器ID列表
}
```

#### 公共方法

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func load_save()` | `void` | 从磁盘加载存档 |
| `func save()` | `void` | 写入存档到磁盘 |
| `func reset()` | `void` | 重置为默认数据并落盘 |
| `func get_slot(slot_index: int)` | `Dictionary` | 获取存档槽数据（副本） |
| `func set_slot(slot_index: int, slot_data: Dictionary)` | `void` | 设置存档槽数据 |
| `func is_slot_empty(slot_index: int)` | `bool` | 判断存档槽是否为空 |
| `func create_slot(slot_index: int, slot_name: String)` | `void` | 创建新存档并落盘 |
| `func delete_slot(slot_index: int)` | `void` | 删除存档并落盘 |
| `func select_slot(slot_index: int)` | `bool` | 选择存档（设为 current_slot），返回是否成功 |
| `func get_playtime_text(seconds: float)` | `String` | 秒数格式化为 `H:MM:SS` 或 `MM:SS` |
| `func get_last_played_text(timestamp: int)` | `String` | 时间戳格式化为 `YYYY-MM-DD HH:MM` |

#### 当前存档便捷方法

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func get_current_slot()` | `Dictionary` | 获取当前选中存档槽（未选返回空字典） |
| `func update_current_slot(field: String, value)` | `void` | 更新当前存档某字段（不落盘） |
| `func get_gold()` | `int` | 获取当前存档金币 |
| `func set_gold(value: int)` | `void` | 设置当前存档金币 |
| `func add_gold(value: int)` | `int` | 增减金币，返回新值 |
| `func get_upgrade_level(upgrade_id: String)` | `int` | 获取局外养成等级 |
| `func set_upgrade_level(upgrade_id: String, level: int)` | `void` | 设置局外养成等级 |
| `func get_stat(key: String)` | `int` | 获取统计值（rounds/best_kills） |
| `func set_stat(key: String, value: int)` | `void` | 设置统计值 |

#### 设置

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func get_setting(key: String, default = null)` | `Variant` | 获取设置项 |
| `func set_setting(key: String, value)` | `void` | 设置并立即落盘 |

**设置项 key**：`master_volume` / `music_volume` / `sfx_volume`（float 0~1）/ `resolution`（String）/ `window_mode`（String）/ `lang`（String）

---

## 二、核心类（class_name 可全局引用）

### 2.1 UIBase（UI 基类）

- **继承**：`Control`
- **路径**：`Scripts/ui/UIBase.gd`
- **职责**：所有 UI 场景的基类，提供生命周期方法
- **`_ready`**：非 UIManager 管理时（如 change_scene_to_file 直接加载）自动调用 on_create/on_open
- **`_ui_managed`**：UIManager 管理时设为 true，_ready 不自动初始化

| 方法签名 | 说明 |
|---|---|
| `func on_create()` | 首次创建时调用（子类在此绑定按钮信号） |
| `func on_open()` | 每次打开时调用（子类在此刷新数据） |
| `func on_close()` | 每次关闭时调用 |
| `func on_destroy()` | 销毁时调用（queue_free） |
| `func _add_hover(btn, brightness=1.3)` | 按钮背景加亮 + hover 高亮（正常态 1.15，hover 态 brightness） |
| `func _refresh_gold_label(label)` | 刷新金币标签 |
| `func _create_list_row(min_height, ...)` | 创建列表行 Panel |
| `func _clear_container(container)` | 清理容器子节点 |

**子类约定**：`on_open/on_close/on_destroy` 覆盖时必须调 `super()`

### 2.2 TableDB（表查询）

- **继承**：`RefCounted`
- **路径**：`Scripts/core/table_db.gd`
- **职责**：运行时表查询，惰性加载并缓存 `.tres` 表资源

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `static func get_table(table_name: String)` | `TableResource` | 获取表资源（按名缓存） |
| `static func rows_of(table_name: String)` | `Array[Dictionary]` | 获取整张表所有行 |
| `static func get_all(table_name: String, field: String, value)` | `Array[Dictionary]` | 按字段值筛选全部匹配行 |
| `static func get_first(table_name: String, field: String, value)` | `Dictionary` | 按字段值取第一条（无匹配返回空字典） |
| `static func clear_cache()` | `void` | 清空缓存（导表后刷新用） |

**使用示例**：
```gdscript
var rows: Array[Dictionary] = TableDB.rows_of("Ducks")
var duck: Dictionary = TableDB.get_first("Ducks", "duckID", 1)
var skills: Array[Dictionary] = TableDB.get_all("skillLevel", "Id", 1)
```

### 2.3 TableResource（表资源）

- **继承**：`Resource`（`@tool`）
- **路径**：`Scripts/core/table_resource.gd`
- **职责**：导表工具产出的 `.tres` 资源格式

| 属性 | 类型 | 说明 |
|---|---|---|
| `var table_name: String` | `String` | 表名（CSV 文件名去扩展名） |
| `var rows: Array[Dictionary]` | `Array[Dictionary]` | 行数据（保留重复 Id） |

### 2.4 BaseConfig（基础配置资源）

- **继承**：`Resource`（`@tool`）
- **路径**：`Scripts/core/base_config.gd`
- **实例**：`res://Resources/Config/base_config.tres`

| 分类 | 属性 | 类型 | 默认值 |
|---|---|---|---|
| 单局 | `round_time` | `float` | `60.0` |
| 玩家基础 | `base_attack` | `float` | `10.0` |
| 玩家基础 | `base_attack_speed` | `float` | `1.0` |
| 玩家基础 | `base_crit_rate` | `float` | `0.05` |
| 玩家基础 | `base_crit_dmg` | `float` | `1.5` |
| 刷怪 | `spawn_interval` | `float` | `2.0` |
| 刷怪 | `spawn_per_wave` | `int` | `1` |
| Boss | `boss_nodes` | `Array[int]` | `[20, 50, 100, 250]` |
| 局内Buff | `buff_trigger_kills` | `Array[int]` | `[10, 25, 45]` |
| 局内Buff | `buff_choice_count` | `int` | `3` |

### 2.5 ConfirmDialog（可复用确认对话框）

- **继承**：`Control`
- **路径**：`Scripts/ui/confirm_dialog.gd` → `Scenes/ui/confirm_dialog.tscn`
- **职责**：模态二次确认对话框

| 方法签名 | 说明 |
|---|---|
| `func setup(title_text: String, message_text: String, confirm_cb: Callable, cancel_cb: Callable = Callable())` | 设置标题、描述、确认/取消回调 |

**使用示例**：
```gdscript
var dialog: ConfirmDialog = load("res://Scenes/ui/confirm_dialog.tscn").instantiate()
dialog.setup("确认删除", "确定删除存档？", func(): do_delete())
add_child(dialog)
```

---

## 三、UI 场景一览

所有 UI 场景存于 `Scenes/ui/`，脚本存于 `Scripts/ui/`，均继承 `UIBase`。

| 场景文件 | 脚本 | 说明 | 导航出口 |
|---|---|---|---|
| `start_ui.tscn` | `startui.gd` | 主菜单（开始/设置/退出） | → save_select / settings |
| `settings.tscn` | `settings.gd` | 设置（分辨率/窗口/音量），即时保存 | → 返回 start_ui |
| `save_select.tscn` | `save_select.gd` | 存档选择（3 槽，继续/删除） | → preparation / start_ui |
| `preparation.tscn` | `preparation.gd` | 备战主界面（局外养成/商店/装备/开始战斗） | → out_of_battle_upgrade / shop / equipment / battle |
| `out_of_battle_upgrade.tscn` | `out_of_battle_upgrade.gd` | 局外属性强化 | → 返回 preparation |
| `shop.tscn` | `shop.gd` | 武器购买解锁 | → 返回 preparation |
| `equipment.tscn` | `equipment.gd` | 武器装备选择 | → 返回 preparation |
| `confirm_dialog.tscn` | `confirm_dialog.gd` | 可复用确认对话框（非全屏UI） | 实例化使用 |
| `battle.tscn` | `battle_manager.gd` | 战斗场景（占位） | → 返回 GameManager |

### UI 导航流程

```
GameManager.tscn（入口）
  └→ start_ui（主菜单）
       ├→ settings（设置，可返回）
       └→ save_select（存档选择）
            ├→ start_ui（返回）
            └→ preparation（备战）
                 ├→ out_of_battle_upgrade（局外养成，可返回）
                 ├→ shop（商店，可返回）
                 ├→ equipment（装备，可返回）
                 └→ battle（战斗，change_scene_to_file）
```

---

## 四、数据表结构

导表工具：`addons/table_exporter/`（编辑器插件，工具栏「导表」按钮）
CSV 源表：`data/*.csv` → 导出 `.tres`：`Resources/Tables/<表名>.tres`

| 表名 | 字段（类型） |
|---|---|
| `Ducks` | `duckID:int` · `duckName:string` · `healthNum:float` · `coin:float` · `moveSpeed:float` · `texture:string` · `spriteFrames:string` |
| `Skill` | `Id:int` · `skillName:string` · `desc:string` · `previouId:int` · `maxLevel:int` |
| `generateProbability` | `waveNumber:int` · `duckId:int` · `name:string` · `weight:int` |
| `skillLevel` | `Id:int` · `skillLevel:int` · `upCost:float` · `changeAttr1~3:string` · `attrValue1~3:float` · `specialEffect:string` |

**CSV 格式**：第1行字段名 / 第2行类型 / 第3行中文备注 / 第4行默认值（后两行导表跳过但必须保留）/ 第5行起数据

---

## 五、文件目录结构

```
2DIncrementGame_Main/
├── Scenes/
│   ├── GameManager.tscn          # 入口场景
│   ├── battle.tscn              # 战斗场景
│   └── ui/                      # 所有 UI 场景
├── Scripts/
│   ├── core/                    # 核心系统
│   │   ├── base_config.gd       # BaseConfig 资源类
│   │   ├── config_system.gd     # ConfigSystem 单例
│   │   ├── save_system.gd       # SaveSystem 单例
│   │   ├── table_db.gd          # TableDB 查询
│   │   └── table_resource.gd    # TableResource 资源类
│   ├── manager/                 # 管理器
│   │   ├── Gamemanager.gd       # 游戏入口控制器
│   │   ├── UIManager.gd         # UIManager 单例
│   │   └── battle_manager.gd    # 战斗场景管理
│   └── ui/                      # UI 脚本
│       ├── UIBase.gd            # UI 基类
│       ├── startui.gd
│       ├── settings.gd
│       ├── save_select.gd
│       ├── preparation.gd
│       ├── out_of_battle_upgrade.gd
│       ├── shop.gd
│       ├── equipment.gd
│       └── confirm_dialog.gd
├── data/                        # CSV 源表
├── Resources/
│   ├── Tables/                  # 导出的 .tres 表资源
│   └── Config/                  # base_config.tres
├── Textures/
│   ├── ui/                      # UI 纹理（menu_bg/btn_start/icon_coin 等）
│   └── PNG/                     # 像素图标素材库
└── addons/table_exporter/       # 导表插件
```

---

## 六、UI 统一规范（像素风）

- **根节点**：`Control` + `anchors_preset=15`（全屏锚点）
- **TopBar**：`HBoxContainer` + `anchor_right=1.0` + `offset_bottom=60`（高度 60）
- **MainContainer**：`MarginContainer` + `anchor_top=1.0` + `anchor_right=1.0` + `anchor_bottom=1.0` + `offset_top=60` + `margin_left=80` + `margin_top=20` + `margin_right=80` + `margin_bottom=40`
- **背景**：`TextureRect + menu_bg.jpg`（禁用 ColorRect）
- **标题**：黄色 `(1, 0.85, 0.2)` + 黑色描边 `outline_size=4` + 字号 36
- **按钮**：`TextureButton`（`btn_start.jpg`）+ `Label` 文字标识，子节点 `mouse_filter=2`
- **面板**：`StyleBoxFlat` 深色背景 `(0.08, 0.1, 0.14, 0.92)` + 边框 + 圆角 8
- **hover 效果**：所有按钮调用 `_add_hover(btn)`，正常态 `modulate=1.15`（更显眼），hover 态 `modulate=1.3`
- **资源路径**：`Textures/ui/`，命名如 `menu_bg.jpg` / `btn_start.jpg` / `icon_coin.jpg`

---

## 七、游戏输入

| 动作名 | 输入类型 | 说明 |
|---|---|---|
| `click` | 鼠标左键 | 游戏内点击/射击 |

注册于 `project.godot` 的 `[input]` 段。
