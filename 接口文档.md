# 接口文档

> 用途：整理本项目所有代码的公共接口，供后续开发参考。
> 约定：写代码前阅读本文件和 `AI开发规范.md`；`项目进程汇报.md` 只提供当前状态速览。

---

## 一、Autoload 单例（全局可访问）

注册于 `project.godot` 的 `[autoload]` 段，可在任意脚本中直接通过单例名访问。

### 1.1 UIManager（UI 管理器）

- **继承**：`Node`
- **路径**：`Scripts/manager/UIManager.gd`
- **职责**：管理 UI 场景的创建、显示、隐藏和缓存

| 方法签名 | 说明 |
|---|---|
| `func initialize(root: Node)` | 初始化 UI 根节点（由 GameManager 调用） |
| `func open_ui(ui_name: String, path: String)` | 打开/显示 UI；首次实例化，后续调用 `show()` + `refresh()` |
| `func close_ui(ui_name: String)` | 使用 `hide()` 隐藏 UI，不销毁缓存 |
| `func clear_all()` | 对缓存 UI 调用 `queue_free()` 并清空缓存 |

**使用示例**：
```gdscript
UIManager.open_ui("save_select", "res://Scenes/ui/save_select.tscn")
UIManager.close_ui("start_ui")
```

### 1.2 ConfigSystem（基础配置）

- **继承**：`Node`
- **路径**：`Scripts/core/config_system.gd`
- **职责**：启动时加载 `res://Resources/Config/base_config.tres`

| 属性 | 类型 | 说明 |
|---|---|---|
| `var config: BaseConfig` | `BaseConfig` | 基础配置实例，只读访问 |

**使用示例**：
```gdscript
var atk: float = ConfigSystem.config.base_attack
var interval: float = ConfigSystem.config.spawn_interval
```

### 1.3 SaveSystem（存档系统）

- **继承**：`Node`
- **路径**：`Scripts/core/save_system.gd`
- **职责**：管理 `user://save.json`，支持 3 个独立存档槽

#### 常量
| 常量 | 值 | 说明 |
|---|---|---|
| `SAVE_PATH` | `"user://save.json"` | 存档文件路径 |
| `SAVE_VERSION` | `2` | 存档版本号 |
| `SLOT_COUNT` | `3` | 存档槽数量 |

#### 存档槽结构（Dictionary）
```gdscript
{
    "id": int,              # 槽位序号 0/1/2
    "name": String,         # 存档名（空字符串=空槽）
    "created_at": int,      # 创建时间戳
    "last_played": int,     # 上次游玩时间戳
    "playtime": float,      # 累计游玩秒数
    "gold": int,            # 金币
    "skill_levels": Dictionary,  # 技能等级 {skill_id: level}（局外养成）
    "owned_weapons": Array,      # 已购买武器 id 列表（默认武器不写入，见 WeaponService）
    "equipped_weapon": int,      # 当前装备武器 id（0=未记录，回退默认武器）
    "stats": Dictionary,    # 统计 {rounds: int, best_kills: int}
}
```

#### 公共方法

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func load_save()` | `void` | 从磁盘加载存档 |
| `func save()` | `void` | 写入存档到磁盘 |
| `func reset()` | `void` | 重置为默认数据并落盘 |
| `func get_slot(slot_index: int)` | `Dictionary` | 获取存档槽数据（副本） |
| `func set_slot(slot_index: int, slot_data: Dictionary)` | `void` | 设置存档槽数据 |
| `func is_slot_empty(slot_index: int)` | `bool` | 判断存档槽是否为空 |
| `func create_slot(slot_index: int, slot_name: String)` | `void` | 创建新存档并落盘 |
| `func delete_slot(slot_index: int)` | `void` | 删除存档并落盘 |
| `func select_slot(slot_index: int)` | `bool` | 选择存档（设为 current_slot），返回是否成功 |
| `func get_playtime_text(seconds: float)` | `String` | 秒数格式化为 `H:MM:SS` 或 `MM:SS` |
| `func get_last_played_text(timestamp: int)` | `String` | 时间戳格式化为 `YYYY-MM-DD HH:MM` |

#### 当前存档便捷方法

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func get_current_slot()` | `Dictionary` | 获取当前选中存档槽（未选返回空字典） |
| `func update_current_slot(field: String, value)` | `void` | 更新当前存档某字段（不落盘） |
| `func get_gold()` | `int` | 获取当前存档金币 |
| `func set_gold(value: int)` | `void` | 设置当前存档金币 |
| `func add_gold(value: int)` | `int` | 增减金币，返回新值 |
| `func get_skill_level(skill_id: int)` | `int` | 获取技能等级（局外养成） |
| `func set_skill_level(skill_id: int, level: int)` | `void` | 设置技能等级（不落盘） |
| `func get_owned_weapons()` | `Array` | 已购买武器 id 列表（不含表内默认武器） |
| `func add_owned_weapon(weapon_id: int)` | `void` | 记录一把已购武器（不落盘，已存在则忽略） |
| `func get_equipped_weapon()` | `int` | 存档记录的装备武器 id（0=未记录） |
| `func set_equipped_weapon(weapon_id: int)` | `void` | 设置装备武器（不落盘） |
| `func get_stat(key: String)` | `int` | 获取统计值（rounds/best_kills） |
| `func set_stat(key: String, value: int)` | `void` | 设置统计值 |

#### 设置

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func get_setting(key: String, default = null)` | `Variant` | 获取设置项 |
| `func set_setting(key: String, value)` | `void` | 设置并立即落盘 |

**设置项 key**：`master_volume` / `music_volume` / `sfx_volume`（float 0~1）/ `resolution`（String）/ `window_mode`（String）/ `lang`（String）

### 1.4 DebugSystem（调试系统）

- **继承**：`Node`
- **路径**：`Scripts/debug/debug_system.gd`
- **职责**：游戏内调试控制台（反引号 ` 开关）。**仅 `OS.has_feature("editor")` 生效；发布包自动禁用**，配合导出排除即不被打包
- 指令：`gold <数量>` / `config <key> <value>`（运行时改 base_config）/ `buff <id> [等级]`（仅战斗）/ `help` / `clear`
- 完整指令说明见项目根目录 **`调试指令.md`**

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `func execute_command(line: String)` | `String` | 执行一条调试指令并返回结果文本（可直接调用/测试） |

---

## 二、核心类（class_name 可全局引用）

### 2.1 UIBase（UI 基类）

- **继承**：`Control`
- **路径**：`Scripts/ui/UIBase.gd`
- **职责**：所有全屏 UI 的基类，提供统一 Button Theme、刷新钩子和列表辅助方法

| 方法签名 | 说明 |
|---|---|
| `func _ready()` | 应用统一 Theme，并触发首次 `refresh()`；子类覆盖时必须调用 `super()` |
| `func refresh()` | UIManager 重新显示缓存界面时刷新动态数据 |
| `func _refresh_gold_label(label)` | 刷新金币标签 |
| `func _create_list_row(min_height, ...)` | 创建列表行 Panel |
| `func _clear_container(container)` | 清理容器子节点 |
| `func _create_text_button(label_text, min_size, font_color=白)` | 创建使用统一 Theme 的 Godot `Button` |
| `func _create_empty_hint(text)` | 创建列表为空时的居中提示 Label |

**常量**：`BUTTON_THEME`（`Resources/button_theme.tres`）

### 2.2 TableDB（表查询）

- **继承**：`RefCounted`
- **路径**：`Scripts/core/table_db.gd`
- **职责**：运行时表查询，惰性加载并缓存 `.tres` 表资源

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `static func get_table(table_name: String)` | `TableResource` | 获取表资源（按名缓存） |
| `static func rows_of(table_name: String)` | `Array[Dictionary]` | 获取整张表所有行 |
| `static func get_all(table_name: String, field: String, value)` | `Array[Dictionary]` | 按字段值筛选全部匹配行 |
| `static func get_first(table_name: String, field: String, value)` | `Dictionary` | 按字段值取第一条（无匹配返回空字典） |
| `static func clear_cache()` | `void` | 清空缓存（导表后刷新用） |

**使用示例**：
```gdscript
var rows: Array[Dictionary] = TableDB.rows_of("Enemy")
var enemy: Dictionary = TableDB.get_first("Enemy", "enemyID", 1)
var levels: Array[Dictionary] = TableDB.get_all("skillLevel", "Id", 1)
```

### 2.3 TableResource（表资源）

- **继承**：`Resource`（`@tool`）
- **路径**：`Scripts/core/table_resource.gd`
- **职责**：导表工具产出的 `.tres` 资源格式

| 属性 | 类型 | 说明 |
|---|---|---|
| `var table_name: String` | `String` | 表名（xlsx 源文件名去扩展名） |
| `var rows: Array[Dictionary]` | `Array[Dictionary]` | 行数据（保留重复 Id） |

### 2.4 BaseConfig（基础配置资源）
- **继承**：`Resource`（`@tool`）
- **路径**：`Scripts/core/base_config.gd`
- **实例**：`res://Resources/Config/base_config.tres`

| 分类 | 属性 | 类型 | 默认值 |
|---|---|---|---|
| 单局 | `round_time` | `float` | `60.0` |
| 玩家基础 | `base_attack` | `float` | `2.0` |
| 玩家基础 | `base_attack_speed` | `float` | `1.0` |
| 玩家基础 | `base_crit_rate` | `float` | `0.05` |
| 玩家基础 | `base_crit_dmg` | `float` | `1.5` |
| 刷怪 | `spawn_interval` | `float` | `1.8` |
| 刷怪 | `spawn_per_wave` | `int` | `1` |
| 刷怪 | `max_enemies` | `int` | `15` |
| 局内Buff | `buff_choice_count` | `int` | `3` |
| 经验 | `exp_base` | `float` | `6.0` |
| 经验 | `exp_power` | `float` | `1.4` |
| 经验 | `exp_linear` | `float` | `8.0` |
| 局外收益 | `exp_gain_rate` | `float` | `0.0` |
| 局外收益 | `coin_gain_rate` | `float` | `0.0` |
| 战斗 | `bullet_speed` | `float` | `1080.0` |
| 战斗 | `spread_half_angle` | `float` | `0.03` |
| 战斗 | `muzzle_offset` | `float` | `26.0` |

**玩家战斗属性计算约定（叠加模型，battle_manager / player_stats_service 实现）**：
- 单发基础伤害 = `base_attack` + 武器 `atk` + 局外技能/Buff 加成（`_damage_bonus`）
- 攻击间隔（秒）= `1.0 / (base_attack_speed × 武器 atkSpeed)`（下限 0.05）
- 暴击：每发子弹独立判定，`randf() < base_crit_rate` 时本发伤害 × `base_crit_dmg`；**暴击率超过 100% 时，每 1% 溢出暴击率转化为 1.5% 暴击伤害**（隐藏机制，不显示在面板）
- 空手兜底：无可用武器时武器 atk=0 / atkSpeed=1.0，即伤害 = base_attack、射速 = base_attack_speed
- 属性面板统一用 `PlayerStatsService`（攻击力/攻击速度/暴击/暴击伤害/体力/子弹分裂/攻击轮数），Buff 加成以（）显示
- 局外技能可用 `changeAttr` 增强 base_config 字段；战斗开始先 `duplicate(true)`，所有修改只作用于本局副本。

### 2.5 WaveEventConfig（波次事件配置资源）

- **继承**：`Resource`（`@tool`）
- **路径**：`Scripts/core/wave_event_config.gd`
- **实例**：`Resources/Events/wave_event_<id>.tres`（每个 waveEvents 事件一个，改数值即可配平，不动代码）
- **职责**：配置波次事件的全部数值（持续时长、落雷/陨石/瘟疫间隔与伤害、老虎机奖励与精英倍率等）

| 分组 | 字段 | 说明 |
|---|---|---|
| 通用 | `event_name` / `effect_time` | 事件名 / 效果持续秒数（<=0 整波生效） |
| 雷暴天 | `thunder_interval_min/max`、`thunder_count_min/max`、`thunder_boss_damage_ratio` | 落雷间隔 / 每次数量 / Boss 扣血比例（非 Boss 秒杀） |
| 火山爆发 | `meteor_interval_min/max`、`meteor_count_min/max`、`meteor_damage_ratio`、`burn_damage_ratio`、`burn_duration`、`meteor_radius` | 陨石间隔 / 数量 / 直接伤害比例 / 点燃每秒伤害 / 点燃时长 / 命中半径 |
| 狂风瘟疫 | `plague_interval_min/max`、`plague_damage_ratio` | 吹风间隔 / 感染每秒伤害 |
| 老虎机 | `slot_spawn_delay`、`slot_elite_ratio`、`slot_elite_hp_ratio`、`slot_elite_reward_ratio`、`slot_reward_gold` | 出现延迟 / 精英概率 / 精英血量与奖励倍率 / 奖励金币 |

### 2.6 ConfirmDialog（可复用确认对话框）

- **继承**：Godot `ConfirmationDialog`
- **路径**：`Scripts/ui/confirm_dialog.gd` → `Scenes/ui/confirm_dialog.tscn`
- **职责**：为原生确认对话框设置标题、描述和一次性回调

| 方法签名 | 说明 |
|---|---|
| `func setup(title_text: String, message_text: String, confirm_cb: Callable, cancel_cb: Callable = Callable())` | 设置标题、描述、确认/取消回调 |

**使用示例**：
```gdscript
var dialog: ConfirmDialog = load("res://Scenes/ui/confirm_dialog.tscn").instantiate()
add_child(dialog)
dialog.setup("确认删除", "确定删除存档？", func(): do_delete())
```

### 2.6 WeaponService（武器域服务）

- **继承**：`RefCounted`（全静态，无需实例化）
- **路径**：`Scripts/core/weapon_service.gd`
- **职责**：把 `weapons` 表与存档拼成运行时武器状态，供商店 / 武器界面 / 未来战斗层共用
- **拥有判定**：`是否拥有 = 表内 isDefault=1 的默认武器 ∪ 存档 owned_weapons`
  → 默认武器由表决定、**不写入存档**，改表即生效，也不需要存档迁移

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `static func get_all()` | `Array[Dictionary]` | 全部武器行（表内顺序） |
| `static func get_by_id(weapon_id: int)` | `Dictionary` | 按 id 取表行（无匹配返回空字典） |
| `static func get_default_ids()` | `Array[int]` | 表内 `isDefault=1` 的武器 id 列表 |
| `static func is_default(weapon_id: int)` | `bool` | 是否为表内默认武器 |
| `static func is_owned(weapon_id: int)` | `bool` | 是否已拥有（默认武器 + 已购买） |
| `static func get_equipped_id()` | `int` | 实际生效的装备武器 id（存档值失效时回退首个默认武器） |
| `static func equip(weapon_id: int)` | `bool` | 装备武器；未拥有返回 false。**不落盘** |
| `static func grant(weapon_id: int)` | `void` | 授予武器（购买成功后调用）。**不落盘** |
| `static func get_equipped_stats()` | `Dictionary` | 当前装备武器的表行（供战斗层读属性） |
| `static func get_dps(row: Dictionary)` | `float` | 每秒理论伤害 = `atk × atkSpeed` |

**字段名常量**：`FIELD_ID`/`FIELD_NAME`/`FIELD_DESC`/`FIELD_ATK`/`FIELD_ATK_SPEED`/`FIELD_IS_DEFAULT`/`FIELD_TEXTURE`

**使用示例**：
```gdscript
if WeaponService.is_owned(2):
    WeaponService.equip(2)
    SaveSystem.save()          # equip/grant 不落盘，调用方负责
var stats: Dictionary = WeaponService.get_equipped_stats()
```

> `equip` / `grant` 均**不落盘**，与 SaveSystem 的 `set_*` 约定一致，由调用方统一 `SaveSystem.save()`（商店购买可一次落盘金币 + 武器）。

### 2.7 PlayerStatsService（玩家属性汇总）

- **继承**：`RefCounted`（全静态，无需实例化）
- **路径**：`Scripts/core/player_stats_service.gd`
- **职责**：汇总基础配置、武器、局外技能与可选的局内 Buff，并为武器/暂停界面生成统一属性文本

| 方法签名 | 返回 | 说明 |
|---|---|---|
| `static func compute(core_base, bonuses, weapon, split_override=-1, burst_override=-1)` | `Dictionary` | 计算攻击、攻速、暴击、暴伤、体力、子弹分裂和攻击轮数 |
| `static func get_meta_stats()` | `Dictionary` | 取得不含局内 Buff 的局外属性 |
| `static func get_effective_crit(crit_rate, crit_dmg)` | `Dictionary` | 暴击率封顶 100%，溢出部分按 1:1.5 转为暴击伤害 |
| `static func format_stats(stats)` | `String` | 格式化属性面板文本，支持 Buff 加成后缀 |

---

## 三、UI 场景一览

所有全屏 UI 场景存于 `Scenes/ui/`，脚本存于 `Scripts/ui/`，均继承 `UIBase`（`confirm_dialog` 除外）。

| 场景文件 | 脚本 | 说明 | 导航出口 |
|---|---|---|---|
| `start_ui.tscn` | `startui.gd` | 主菜单（开始/设置/退出） | → save_select / settings |
| `settings.tscn` | `settings.gd` | 设置（分辨率/窗口/音量），即时保存 | → 返回 start_ui |
| `save_select.tscn` | `save_select.gd` | 存档选择（实例化 3 个 `save_slot.tscn`） | → preparation / start_ui |
| `preparation.tscn` | `preparation.gd` | 备战界面（返回/局外养成/商店/武器/开始讨伐） | → save_select / battle / 三个子界面 |
| `out_of_battle_upgrade.tscn` | `out_of_battle_upgrade.gd` | 局外养成（左侧传统技能树 + 右侧技能详情与升级：Skill 表 previouId 分层，点击节点查看 Skill/skillLevel 表文本，升级消耗金币；连线由 skill_tree_canvas.gd 绘制） | → 返回 preparation |
| `shop.tscn` | `shop.gd` | 商店（shop 表，金币购买武器，限购置灰） | → 返回 preparation |
| `weapon.tscn` | `weapon.gd` | 武器（weapons 表，展示玩家/武器属性并装备已拥有武器） | → 返回 preparation |
| `save_slot.tscn` | — | 可复用存档槽布局 | 由 save_select 实例化 |
| `confirm_dialog.tscn` | `confirm_dialog.gd` | Godot 原生确认对话框（非全屏 UI） | 实例化使用 |
| `battle.tscn` | `Scripts/battle/battle_manager.gd` | 战斗场景（射击/敌人刷怪/Boss 波次/局内 Buff 三选一/暂停属性/金币与结算） | → 返回 preparation |

### UI 导航流程

```
GameManager.tscn（入口）
  └→ start_ui（主菜单）
       ├→ settings（设置，可返回）
       └→ save_select（存档选择）
            ├→ start_ui（返回）
            └→ preparation（备战）
                 ├→ out_of_battle_upgrade（局外养成，可返回）
                 ├→ shop（商店，可返回）
                 ├→ weapon（武器，可返回）
                 ├→ save_select（返回）
                 └→ battle（战斗，change_scene_to_file）
```

---

## 四、数据表结构

导表工具：`addons/table_exporter/`（编辑器插件，工具栏「导表」按钮）
源表：`data/*.xlsx`（当前 10 张表全部为 xlsx）→ 导出 `.tres`：`Resources/Tables/<表名>.tres`

| 表名 | 源文件 | 字段（类型） |
|---|---|---|
| `Enemy` | `Enemy.xlsx` | `enemyID:int` · `enemyName:string` · `healthNum:float` · `coin:float` · `exp:float` · `moveSpeed:float` · `texture:string` · `spriteFrames:string` |
| `Skill` | `Skill.xlsx` | `Id:int` · `skillName:string` · `desc:string` · `previouId:int` · `maxLevel:int` |
| `generateProbability` | `generateProbability.xlsx` | `waveNumber:int` · `enemyId:int` · `name:string` · `weight:int` |
| `waveBoss` | `waveBoss.xlsx` | `waveNumber:int` · `enemyId:int` · `name:string` · `CreateCost:int` · `weight:int` |
| `waveEvents` | `waveEvents.xlsx` | `eventsId:int` · `name:string` · `desc:string` · `weight:int` · `effectTime:float` |
| `skillLevel` | `skillLevel.xlsx` | `Id:int` · `skillLevel:int` · `upCost:float` · `changeAttr1~4:string` · `attrValue1~4:float` · `desc:string` · `specialEffect:string` |
| `shop` | `shop.xlsx` | `Id:int` · `itemType:string` · `refId:int` · `propName:string` · `costNum:int` · `purchaseLimit:int` |
| `weapons` | `weapons.xlsx` | `weaponId:int` · `weaponName:string` · `desc:string` · `atk:float` · `atkSpeed:float` · `isDefault:int` · `texture:string` |
| `Buff` | `Buff.xlsx` | `Id:int` · `buffName:string` · `desc:string` · `maxLevel:int` · `weight:int` |
| `buffLevel` | `buffLevel.xlsx` | `Id:int` · `buffLevel:int` · `changeAttr1~4:string` · `attrValue1~4:float` · `desc:string` · `specialEffect:string` |

**shop 表字段语义**：`itemType` 商品类型（当前仅 `weapon`）· `refId` 关联对象 id（`weapon` 类型指向 `weapons.weaponId`）· `purchaseLimit` 限购次数（`0`=不限次，`1`=仅一次）
**weapons 表字段语义**：`isDefault=1` 表示开局默认拥有（不写存档，改表即生效）· `texture` 静态图片（当前为空，界面暂不显示图标）
**skillLevel 表字段语义**：`changeAttr1~4` 支持战斗内部属性和 base_config 字段名；战斗修改每局独立的配置副本。
**waveBoss 表字段语义**：每波一行——`waveNumber` 波数 · `enemyId` 关联 `Enemy.enemyID` · `CreateCost` Boss 生成门槛；`weight` 字段仍在源表中，但当前唯一行模式不参与运行时抽取。
**waveEvents 表字段语义**：进入下一波时按 `weight` 加权随机触发一次——`eventsId` 事件 id（对应 `Resources/Events/wave_event_<id>.tres` 配置）· `desc` 描述 · `effectTime` 预留（实际持续时长以配置文件为准）。

**五段式源表格式**：第1行字段名 / 第2行类型 / 第3行中文备注 / 第4行默认值（后两行导表跳过但必须保留）/ 第5行起数据

**源表格式**：
- **统一用 `.xlsx`**：内部是 UTF-8 XML，无中文编码问题，也不会被 Godot 误当翻译表导入。
- `~$xxx.xlsx`（Excel/WPS 打开时的锁文件）自动跳过。
- 导表前请在 Excel/WPS 中关闭该文件，否则 ZIPReader 无法打开

---

## 五、目录与 UI 约定

目录结构和视觉规范只在 [AI开发规范.md](./AI开发规范.md) 第三章、第八章维护，本文件不再复制。

当前接口相关约定：

- 全屏界面继承 `UIBase`，导航使用 `UIManager`。
- 存档选择界面实例化三个 `save_slot.tscn`。
- 确认操作使用 Godot `ConfirmationDialog`。
- 普通按钮使用 `Button` 并继承 `Resources/button_theme.tres`；动态按钮调用 `UIBase._create_text_button()`。
- 商店、武器与局外养成的 `ScrollContainer` 使用 `DragScroll` 支持鼠标拖拽。

---

## 六、游戏输入

| 动作名 | 输入类型 | 说明 |
|---|---|---|
| `click` | 鼠标左键 | 游戏内点击/射击 |

注册于 `project.godot` 的 `[input]` 段。
