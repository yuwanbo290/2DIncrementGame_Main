# 调试系统指令

> 调试系统（Autoload：`DebugSystem`）仅在**编辑器/调试构建**中启用（`OS.has_feature("editor")`），**发布包不会提供任何调试功能**。
> 打开方式：游戏运行中按反引号键 `` ` ``（Tab 键上方）开关控制台；在输入框输入指令后按回车执行。

## 指令列表

| 指令 | 说明 | 示例 |
|---|---|---|
| `gold <数量>` | 添加金币（正数增加 / 负数扣减），立即落盘 | `gold 500`、`gold -100` |
| `config <key> <value>` | 运行时调整 base_config 数值（**仅本次运行生效，不写回文件**） | `config spawn_interval 1.5` |
| `buff <id> [目标等级]` | **仅战斗内可用**：为 Buff 添加/提升等级；不填等级默认升 1 级 | `buff 1`、`buff 2 3` |
| `clear` | 清空控制台输出 | `clear` |
| `help` | 显示全部指令 | `help` |

## config 可用字段（`Resources/Config/base_config.tres`）

| key | 类型 | 当前默认 | 说明 |
|---|---|---|---|
| `round_time` | float | `60.0` | 单局时间（秒） |
| `base_attack` | float | `10.0` | 玩家基础攻击力 |
| `base_attack_speed` | float | `1.0` | 基础攻速（次/秒） |
| `base_crit_rate` | float | `0.05` | 暴击率（0~1） |
| `base_crit_dmg` | float | `1.5` | 暴击伤害倍率 |
| `spawn_interval` | float | `1.8` | 刷怪间隔（秒） |
| `spawn_per_wave` | int | `1` | 每次刷怪数量 |
| `max_enemies` | int | `15` | 场上敌人数量上限（达到后停止刷怪） |
| `buff_choice_count` | int | `3` | 每次升级 Buff 可选数量 |
| `exp_base` | float | `6.0` | 升级经验公式系数（Exp(level) = exp_base × level^exp_power + exp_linear × (level-1)） |
| `exp_power` | float | `1.4` | 升级经验公式指数 |
| `exp_linear` | float | `8.0` | 升级经验公式线性项系数 |
| `exp_gain_rate` | float | `0.0` | 击杀经验获得加成（0.1 = +10%） |
| `coin_gain_rate` | float | `0.0` | 击杀金币获得加成（0.1 = +10%） |
| `bullet_speed` | float | `640.0` | 子弹飞行速度 |
| `spread_half_angle` | float | `0.12` | 多弹散布半角（弧度） |
| `muzzle_offset` | float | `26.0` | 子弹出生点距玩家距离 |

**数组写法**：`config boss_nodes [20, 50, 100]`

## skillLevel 表属性关联 base_config

局外技能（`skillLevel` 表的 `changeAttr1~3`）可直接用 **base_config 字段名**作为属性 key，技能生效后累加对应 `attrValue`。加成仅在**单局战斗内生效**，战斗结束自动还原：

| changeAttr 写法 | 影响的 base_config 字段 | 效果（attrValue 处理） |
|---|---|---|
| `base_attack` | `base_attack` | 基础攻击 +attrValue |
| `base_attack_speed` | `base_attack_speed` | 基础攻速 +attrValue |
| `base_crit_rate` | `base_crit_rate` | 暴击率 +attrValue（0~1） |
| `base_crit_dmg` | `base_crit_dmg` | 暴击伤害倍率 +attrValue |
| `round_time` | `round_time` | 单局时间 +attrValue 秒（进战斗前生效） |
| `spawn_interval` | `spawn_interval` | 刷怪间隔 +attrValue 秒（正数变慢 / 负数变快） |
| `spawn_per_wave` | `spawn_per_wave` | 每次刷怪数量 +attrValue |
| `exp_gain_rate` | `exp_gain_rate` | 经验获取 +attrValue（0.1 = +10%） |
| `coin_gain_rate` | `coin_gain_rate` | 金币获取 +attrValue（0.1 = +10%） |

**用法示例**：`skillLevel` 表某行配置 `changeAttr1 = base_attack, attrValue1 = 3` → 该技能在该等级下基础攻击 +3。

> 注意：
> - key 必须与 base_config 字段名**完全一致**（区分大小写）；写错会在战斗输出「忽略未支持的属性 key」警告并跳过。
> - 与 `atk`/`bulletCount`/`ricochetCount`/`burstCount` 等战斗内部属性共用同一入口（`_apply_attribute_change`），可混合配置。
> - 可用 `config <key> <value>` 指令实时观察/验证这些字段的当前值（进入战斗后即为技能增强后的值）。

## Buff id 对照（`buff` 指令使用，`Buff.xlsx` 表）

| id | 名称 | 效果属性 |
|---|---|---|
| 1 | 火力强化 | base_attack（攻击） |
| 2 | 分裂射击 | bulletCount（散射子弹数） |
| 3 | 弹性子弹 | ricochetCount（弹射次数） |
| 4 | 重型弹头 | base_attack + base_crit_rate（攻击 + 暴击几率） |
| 5 | 弹幕扩容 | burstCount（一次射击连射轮数） |
| 6 | 要害射击 | base_crit_rate（暴击几率） |
| 7 | 爆裂弹头 | base_crit_dmg（暴击伤害） |
| 8 | 疾风速射 | base_attack_speed（攻击速度） |
| 9 | 迅捷爆射 | base_attack_speed + base_crit_rate（攻速 + 暴击率） |
| 10 | 狂暴射击 | base_attack_speed + base_crit_dmg（攻速 + 暴击伤害） |
| 11 | 无尽之力 | base_crit_rate + base_crit_dmg（暴击率 + 暴击伤害） |
| 12 | 明镜止水 | base_attack + base_crit_rate + base_attack_speed + base_crit_dmg |

## 注意事项

- `buff` 指令仅可在**战斗场景**中使用；备战/主菜单等场景会提示"仅可在战斗中添加"
- `config` 修改只影响内存中的运行时配置，**不写回 `.tres` 文件**，重启游戏后恢复
- 调试系统文件位于 `Scripts/debug/`，导出发布包时可一并排除；即使残留，代码内 `OS.has_feature("editor")` 检查也会让它在发布包中完全禁用
