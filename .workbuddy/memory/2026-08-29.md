# 2026-08-29

## 会话：阅读项目准则并核对代码现状
通读三份准则文档 + `调试指令.md`，并核对 `project.godot` / 脚本 / 场景 / 表资源的实际状态。已把项目级约定写入 `MEMORY.md`。

### 核对结果：文档与代码一致的部分
- Godot 4.7 / Forward Plus / d3d12 / `canvas_items`+`expand` / `default_texture_filter=0` 全部符合规范。
- 4 个 Autoload 注册齐全：UIManager、ConfigSystem、SaveSystem、DebugSystem。
- 9 张表 xlsx ↔ tres 一一对应，无孤儿数据。
- `TableDB` / `UIBase` / `PlayerStatsService` / `battle_manager` 代码风格符合规范（显式类型标注、`Dictionary.get` 带默认值、常量提 const、注释用 `##`）。

### 发现的文档滞后 / 待办（未擅自修改，留待用户决策）
1. `接口文档.md` §2.1 UIBase 只登记了 6 个方法，实际实现远多于此：`play_enter` / `play_exit` / `popup_in` / `bind_button` / `reveal_card` / `pulse` / `tween_range` / `count_label` / `get_default_focus` / `is_reduced_motion`。
2. 设置项 key `reduced_motion`（bool，减少动效）已在 `save_system.gd`、`settings.gd`、`UIBase` 中使用，但 `接口文档.md` §1.3 的设置 key 列表未登记。
3. P1 命名整改仍未执行：`Scripts/manager/Gamemanager.gd` 仍为驼峰（`start_ui.tscn` 场景名已规范，但 `Scripts/ui/startui.gd` 尚未改）。
4. 规范第三章目录结构未收录但实际存在的文件：`Scripts/Items/can/can.gd`；`Scripts/debug/refactor_smoke.gd` + `Scenes/refactor_smoke.tscn`（临时验证用途，规范要求提交时排除）。

### 尚未确认的待开发项（规范 §15.2）
关卡随机 modifier、敌人美术资源接入（`Enemy.xlsx.texture` 为空，仍用代码绘制色块）、音频文件缺失。
